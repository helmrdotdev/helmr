# Helmr error text and public API documentation preserve product nouns such as
# Run, Actor, and Workspace. Keep correctness and dead-code checks enabled while
# excluding style rules that conflict with that existing repository convention.
checks = ["all", "-ST1000", "-ST1003", "-ST1005", "-ST1020", "-ST1021"]
