# Fixtures

Non-customer-facing fixtures live here.

- `task-projects/`: runnable Helmr task projects used by runtime and integration
  tests. These projects use the public SDK surface, but they are fixtures rather
  than SDK implementation or customer examples.
