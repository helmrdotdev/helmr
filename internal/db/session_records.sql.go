// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: session_records.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const appendActorInputRecord = `-- name: AppendActorInputRecord :one
WITH selected_claim AS MATERIALIZED (
    SELECT id, state, request_fingerprint
      FROM idempotency_claims
     WHERE idempotency_claims.environment_id = $1::uuid
       AND idempotency_claims.id = $2
       AND idempotency_claims.operation = 'session.input.send'
       AND idempotency_claims.retired_at IS NULL
     FOR UPDATE
), existing_record AS MATERIALIZED (
    SELECT session_records.id, session_records.environment_id, session_records.session_id, session_records.direction, session_records.sequence, session_records.data, session_records.content_type, session_records.source_kind, session_records.source_run_id, session_records.producer_run_id, session_records.producer_attempt_number, session_records.claim_id, session_records.created_at
      FROM session_records
      JOIN selected_claim ON selected_claim.id = session_records.claim_id
     WHERE session_records.session_id = $3
       AND session_records.direction = 'input'
), locked_actor AS MATERIALIZED (
    SELECT sessions.id, sessions.environment_id, sessions.actor_declared_id, sessions.deployment_definition_id, sessions.workspace_id, sessions.key, sessions.current_run_id, sessions.run_generation, sessions.state_version, sessions.manual_run_cancelled, sessions.failure, sessions.failure_run_id, sessions.next_input_sequence, sessions.committed_input_sequence, sessions.next_output_sequence, sessions.input_retention_floor, sessions.output_retention_floor, sessions.run_queue_name, sessions.run_concurrency_key, sessions.run_queue_concurrency_limit, sessions.run_priority, sessions.run_queue_ttl_ms, sessions.run_max_active_duration_ms, sessions.run_retry_policy, sessions.run_metadata, sessions.run_tags, sessions.state, sessions.close_sequence, sessions.created_at, sessions.updated_at, sessions.closed_at, sessions.cancelled_at, sessions.failed_at
      FROM sessions
     WHERE sessions.environment_id = $1
       AND sessions.id = $3
       AND sessions.state = 'open'
       AND sessions.next_input_sequence <= 9007199254740991
       AND NOT EXISTS (SELECT 1 FROM existing_record)
       AND (
           $2::uuid IS NULL
           OR EXISTS (
               SELECT 1
                 FROM selected_claim
                WHERE selected_claim.state = 'pending'
                  AND request_fingerprint = $4
           )
       )
     FOR UPDATE
), allocated AS (
    UPDATE sessions
       SET next_input_sequence = sessions.next_input_sequence + 1,
           manual_run_cancelled = false,
           updated_at = now()
      FROM locked_actor
     WHERE sessions.id = locked_actor.id
    RETURNING sessions.id, sessions.environment_id, sessions.actor_declared_id, sessions.deployment_definition_id, sessions.workspace_id, sessions.key, sessions.current_run_id, sessions.run_generation, sessions.state_version, sessions.manual_run_cancelled, sessions.failure, sessions.failure_run_id, sessions.next_input_sequence, sessions.committed_input_sequence, sessions.next_output_sequence, sessions.input_retention_floor, sessions.output_retention_floor, sessions.run_queue_name, sessions.run_concurrency_key, sessions.run_queue_concurrency_limit, sessions.run_priority, sessions.run_queue_ttl_ms, sessions.run_max_active_duration_ms, sessions.run_retry_policy, sessions.run_metadata, sessions.run_tags, sessions.state, sessions.close_sequence, sessions.created_at, sessions.updated_at, sessions.closed_at, sessions.cancelled_at, sessions.failed_at, sessions.next_input_sequence - 1 AS allocated_sequence
), inserted_record AS (
    INSERT INTO session_records (
        id,
        environment_id,
        session_id,
        direction,
        sequence,
        data,
        content_type,
        source_kind,
        source_run_id,
        claim_id
    )
    SELECT $5,
           allocated.environment_id,
           allocated.id,
           'input',
           allocated.allocated_sequence,
           $6,
           'application/json',
           $7,
           $8,
           $2
      FROM allocated
    RETURNING session_records.id, session_records.environment_id, session_records.session_id, session_records.direction, session_records.sequence, session_records.data, session_records.content_type, session_records.source_kind, session_records.source_run_id, session_records.producer_run_id, session_records.producer_attempt_number, session_records.claim_id, session_records.created_at
)
SELECT inserted_record.id, inserted_record.environment_id, inserted_record.session_id, inserted_record.direction, inserted_record.sequence, inserted_record.data, inserted_record.content_type, inserted_record.source_kind, inserted_record.source_run_id, inserted_record.producer_run_id, inserted_record.producer_attempt_number, inserted_record.claim_id, inserted_record.created_at, false::boolean AS claim_fingerprint_mismatch, true::boolean AS appended
  FROM inserted_record
UNION ALL
SELECT existing_record.id, existing_record.environment_id, existing_record.session_id, existing_record.direction, existing_record.sequence, existing_record.data, existing_record.content_type, existing_record.source_kind, existing_record.source_run_id, existing_record.producer_run_id, existing_record.producer_attempt_number, existing_record.claim_id, existing_record.created_at,
       (selected_claim.request_fingerprint <> $4)::boolean
           AS claim_fingerprint_mismatch,
       false::boolean AS appended
  FROM existing_record
  JOIN selected_claim ON selected_claim.id = existing_record.claim_id
`

type AppendActorInputRecordParams struct {
	EnvironmentID              pgtype.UUID `json:"environment_id"`
	ClaimID                    pgtype.UUID `json:"claim_id"`
	SessionID                  pgtype.UUID `json:"session_id"`
	ExpectedRequestFingerprint []byte      `json:"expected_request_fingerprint"`
	ID                         pgtype.UUID `json:"id"`
	Data                       []byte      `json:"data"`
	SourceKind                 pgtype.Text `json:"source_kind"`
	SourceRunID                pgtype.UUID `json:"source_run_id"`
}

type AppendActorInputRecordRow struct {
	ID                       pgtype.UUID        `json:"id"`
	EnvironmentID            pgtype.UUID        `json:"environment_id"`
	SessionID                pgtype.UUID        `json:"session_id"`
	Direction                string             `json:"direction"`
	Sequence                 int64              `json:"sequence"`
	Data                     []byte             `json:"data"`
	ContentType              string             `json:"content_type"`
	SourceKind               pgtype.Text        `json:"source_kind"`
	SourceRunID              pgtype.UUID        `json:"source_run_id"`
	ProducerRunID            pgtype.UUID        `json:"producer_run_id"`
	ProducerAttemptNumber    pgtype.Int4        `json:"producer_attempt_number"`
	ClaimID                  pgtype.UUID        `json:"claim_id"`
	CreatedAt                pgtype.Timestamptz `json:"created_at"`
	ClaimFingerprintMismatch bool               `json:"claim_fingerprint_mismatch"`
	Appended                 bool               `json:"appended"`
}

func (q *Queries) AppendActorInputRecord(ctx context.Context, arg AppendActorInputRecordParams) (AppendActorInputRecordRow, error) {
	row := q.db.QueryRow(ctx, appendActorInputRecord,
		arg.EnvironmentID,
		arg.ClaimID,
		arg.SessionID,
		arg.ExpectedRequestFingerprint,
		arg.ID,
		arg.Data,
		arg.SourceKind,
		arg.SourceRunID,
	)
	var i AppendActorInputRecordRow
	err := row.Scan(
		&i.ID,
		&i.EnvironmentID,
		&i.SessionID,
		&i.Direction,
		&i.Sequence,
		&i.Data,
		&i.ContentType,
		&i.SourceKind,
		&i.SourceRunID,
		&i.ProducerRunID,
		&i.ProducerAttemptNumber,
		&i.ClaimID,
		&i.CreatedAt,
		&i.ClaimFingerprintMismatch,
		&i.Appended,
	)
	return i, err
}

const appendActorOutputRecord = `-- name: AppendActorOutputRecord :one
WITH selected_claim AS MATERIALIZED (
    SELECT id, state, request_fingerprint
      FROM idempotency_claims
     WHERE idempotency_claims.environment_id = $1::uuid
       AND idempotency_claims.id = $2
       AND idempotency_claims.operation = 'session.output.append'
       AND idempotency_claims.retired_at IS NULL
     FOR UPDATE
), existing_record AS MATERIALIZED (
    SELECT session_records.id, session_records.environment_id, session_records.session_id, session_records.direction, session_records.sequence, session_records.data, session_records.content_type, session_records.source_kind, session_records.source_run_id, session_records.producer_run_id, session_records.producer_attempt_number, session_records.claim_id, session_records.created_at
      FROM session_records
      JOIN selected_claim ON selected_claim.id = session_records.claim_id
     WHERE session_records.session_id = $3
       AND session_records.direction = 'output'
), locked_actor AS MATERIALIZED (
    SELECT sessions.id, sessions.environment_id, sessions.actor_declared_id, sessions.deployment_definition_id, sessions.workspace_id, sessions.key, sessions.current_run_id, sessions.run_generation, sessions.state_version, sessions.manual_run_cancelled, sessions.failure, sessions.failure_run_id, sessions.next_input_sequence, sessions.committed_input_sequence, sessions.next_output_sequence, sessions.input_retention_floor, sessions.output_retention_floor, sessions.run_queue_name, sessions.run_concurrency_key, sessions.run_queue_concurrency_limit, sessions.run_priority, sessions.run_queue_ttl_ms, sessions.run_max_active_duration_ms, sessions.run_retry_policy, sessions.run_metadata, sessions.run_tags, sessions.state, sessions.close_sequence, sessions.created_at, sessions.updated_at, sessions.closed_at, sessions.cancelled_at, sessions.failed_at
      FROM sessions
      JOIN runs
        ON runs.session_id = sessions.id
       AND runs.id = $4
       AND runs.workspace_id = sessions.workspace_id
      JOIN run_attempts
        ON run_attempts.run_id = runs.id
       AND run_attempts.number = $5
       AND run_attempts.workspace_id = sessions.workspace_id
     WHERE sessions.environment_id = $1
       AND sessions.id = $3
       AND sessions.current_run_id = runs.id
       AND sessions.state IN ('open', 'closing')
       AND sessions.next_output_sequence <= 9007199254740991
       AND NOT EXISTS (SELECT 1 FROM existing_record)
       AND (
           $2::uuid IS NULL
           OR EXISTS (
               SELECT 1
                 FROM selected_claim
                WHERE selected_claim.state = 'pending'
                  AND request_fingerprint = $6
           )
       )
     FOR UPDATE OF sessions
), allocated AS (
    UPDATE sessions
       SET next_output_sequence = sessions.next_output_sequence + 1,
           updated_at = now()
      FROM locked_actor
     WHERE sessions.id = locked_actor.id
    RETURNING sessions.id, sessions.environment_id, sessions.actor_declared_id, sessions.deployment_definition_id, sessions.workspace_id, sessions.key, sessions.current_run_id, sessions.run_generation, sessions.state_version, sessions.manual_run_cancelled, sessions.failure, sessions.failure_run_id, sessions.next_input_sequence, sessions.committed_input_sequence, sessions.next_output_sequence, sessions.input_retention_floor, sessions.output_retention_floor, sessions.run_queue_name, sessions.run_concurrency_key, sessions.run_queue_concurrency_limit, sessions.run_priority, sessions.run_queue_ttl_ms, sessions.run_max_active_duration_ms, sessions.run_retry_policy, sessions.run_metadata, sessions.run_tags, sessions.state, sessions.close_sequence, sessions.created_at, sessions.updated_at, sessions.closed_at, sessions.cancelled_at, sessions.failed_at, sessions.next_output_sequence - 1 AS allocated_sequence
), inserted_record AS (
    INSERT INTO session_records (
        id,
        environment_id,
        session_id,
        direction,
        sequence,
        data,
        content_type,
        producer_run_id,
        producer_attempt_number,
        claim_id
    )
    SELECT $7,
           allocated.environment_id,
           allocated.id,
           'output',
           allocated.allocated_sequence,
           $8,
           $9,
           $4,
           $5,
           $2
      FROM allocated
    RETURNING session_records.id, session_records.environment_id, session_records.session_id, session_records.direction, session_records.sequence, session_records.data, session_records.content_type, session_records.source_kind, session_records.source_run_id, session_records.producer_run_id, session_records.producer_attempt_number, session_records.claim_id, session_records.created_at
)
SELECT inserted_record.id, inserted_record.environment_id, inserted_record.session_id, inserted_record.direction, inserted_record.sequence, inserted_record.data, inserted_record.content_type, inserted_record.source_kind, inserted_record.source_run_id, inserted_record.producer_run_id, inserted_record.producer_attempt_number, inserted_record.claim_id, inserted_record.created_at, false::boolean AS claim_fingerprint_mismatch, true::boolean AS appended
  FROM inserted_record
UNION ALL
SELECT existing_record.id, existing_record.environment_id, existing_record.session_id, existing_record.direction, existing_record.sequence, existing_record.data, existing_record.content_type, existing_record.source_kind, existing_record.source_run_id, existing_record.producer_run_id, existing_record.producer_attempt_number, existing_record.claim_id, existing_record.created_at,
       (selected_claim.request_fingerprint <> $6)::boolean
           AS claim_fingerprint_mismatch,
       false::boolean AS appended
  FROM existing_record
  JOIN selected_claim ON selected_claim.id = existing_record.claim_id
`

type AppendActorOutputRecordParams struct {
	EnvironmentID              pgtype.UUID `json:"environment_id"`
	ClaimID                    pgtype.UUID `json:"claim_id"`
	SessionID                  pgtype.UUID `json:"session_id"`
	ProducerRunID              pgtype.UUID `json:"producer_run_id"`
	ProducerAttemptNumber      int32       `json:"producer_attempt_number"`
	ExpectedRequestFingerprint []byte      `json:"expected_request_fingerprint"`
	ID                         pgtype.UUID `json:"id"`
	Data                       []byte      `json:"data"`
	ContentType                string      `json:"content_type"`
}

type AppendActorOutputRecordRow struct {
	ID                       pgtype.UUID        `json:"id"`
	EnvironmentID            pgtype.UUID        `json:"environment_id"`
	SessionID                pgtype.UUID        `json:"session_id"`
	Direction                string             `json:"direction"`
	Sequence                 int64              `json:"sequence"`
	Data                     []byte             `json:"data"`
	ContentType              string             `json:"content_type"`
	SourceKind               pgtype.Text        `json:"source_kind"`
	SourceRunID              pgtype.UUID        `json:"source_run_id"`
	ProducerRunID            pgtype.UUID        `json:"producer_run_id"`
	ProducerAttemptNumber    pgtype.Int4        `json:"producer_attempt_number"`
	ClaimID                  pgtype.UUID        `json:"claim_id"`
	CreatedAt                pgtype.Timestamptz `json:"created_at"`
	ClaimFingerprintMismatch bool               `json:"claim_fingerprint_mismatch"`
	Appended                 bool               `json:"appended"`
}

func (q *Queries) AppendActorOutputRecord(ctx context.Context, arg AppendActorOutputRecordParams) (AppendActorOutputRecordRow, error) {
	row := q.db.QueryRow(ctx, appendActorOutputRecord,
		arg.EnvironmentID,
		arg.ClaimID,
		arg.SessionID,
		arg.ProducerRunID,
		arg.ProducerAttemptNumber,
		arg.ExpectedRequestFingerprint,
		arg.ID,
		arg.Data,
		arg.ContentType,
	)
	var i AppendActorOutputRecordRow
	err := row.Scan(
		&i.ID,
		&i.EnvironmentID,
		&i.SessionID,
		&i.Direction,
		&i.Sequence,
		&i.Data,
		&i.ContentType,
		&i.SourceKind,
		&i.SourceRunID,
		&i.ProducerRunID,
		&i.ProducerAttemptNumber,
		&i.ClaimID,
		&i.CreatedAt,
		&i.ClaimFingerprintMismatch,
		&i.Appended,
	)
	return i, err
}

const commitActorInputCursor = `-- name: CommitActorInputCursor :one
UPDATE sessions
   SET committed_input_sequence = $1,
       state_version = state_version + 1,
       updated_at = now()
 WHERE environment_id = $2
   AND id = $3
   AND current_run_id = $4
   AND run_generation = $5
   AND committed_input_sequence < $1
   AND $1 < next_input_sequence
RETURNING id, environment_id, actor_declared_id, deployment_definition_id, workspace_id, key, current_run_id, run_generation, state_version, manual_run_cancelled, failure, failure_run_id, next_input_sequence, committed_input_sequence, next_output_sequence, input_retention_floor, output_retention_floor, run_queue_name, run_concurrency_key, run_queue_concurrency_limit, run_priority, run_queue_ttl_ms, run_max_active_duration_ms, run_retry_policy, run_metadata, run_tags, state, close_sequence, created_at, updated_at, closed_at, cancelled_at, failed_at
`

type CommitActorInputCursorParams struct {
	CommittedInputSequence int64       `json:"committed_input_sequence"`
	EnvironmentID          pgtype.UUID `json:"environment_id"`
	SessionID              pgtype.UUID `json:"session_id"`
	RunID                  pgtype.UUID `json:"run_id"`
	ExpectedRunGeneration  int64       `json:"expected_run_generation"`
}

func (q *Queries) CommitActorInputCursor(ctx context.Context, arg CommitActorInputCursorParams) (Session, error) {
	row := q.db.QueryRow(ctx, commitActorInputCursor,
		arg.CommittedInputSequence,
		arg.EnvironmentID,
		arg.SessionID,
		arg.RunID,
		arg.ExpectedRunGeneration,
	)
	var i Session
	err := row.Scan(
		&i.ID,
		&i.EnvironmentID,
		&i.ActorDeclaredID,
		&i.DeploymentDefinitionID,
		&i.WorkspaceID,
		&i.Key,
		&i.CurrentRunID,
		&i.RunGeneration,
		&i.StateVersion,
		&i.ManualRunCancelled,
		&i.Failure,
		&i.FailureRunID,
		&i.NextInputSequence,
		&i.CommittedInputSequence,
		&i.NextOutputSequence,
		&i.InputRetentionFloor,
		&i.OutputRetentionFloor,
		&i.RunQueueName,
		&i.RunConcurrencyKey,
		&i.RunQueueConcurrencyLimit,
		&i.RunPriority,
		&i.RunQueueTtlMs,
		&i.RunMaxActiveDurationMs,
		&i.RunRetryPolicy,
		&i.RunMetadata,
		&i.RunTags,
		&i.State,
		&i.CloseSequence,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ClosedAt,
		&i.CancelledAt,
		&i.FailedAt,
	)
	return i, err
}

const completeActorInputClaim = `-- name: CompleteActorInputClaim :one
UPDATE idempotency_claims
   SET state = 'completed',
       receipt = jsonb_build_object(
	       'session_record_id', session_records.id::text,
           'sequence', session_records.sequence
       ),
       completed_at = transaction_timestamp()
  FROM session_records
 WHERE idempotency_claims.environment_id = $1::uuid
   AND idempotency_claims.id = $2
   AND idempotency_claims.operation = 'session.input.send'
   AND idempotency_claims.request_fingerprint = $3
   AND idempotency_claims.state = 'pending'
   AND idempotency_claims.retired_at IS NULL
   AND session_records.environment_id = idempotency_claims.environment_id
   AND session_records.session_id = $4
   AND session_records.id = $5
   AND session_records.direction = 'input'
   AND session_records.claim_id = idempotency_claims.id
RETURNING idempotency_claims.id, idempotency_claims.environment_id, idempotency_claims.operation, idempotency_claims.slot_hash, idempotency_claims.request_fingerprint, idempotency_claims.state, idempotency_claims.receipt, idempotency_claims.accepted_at, idempotency_claims.expires_at, idempotency_claims.retired_at, idempotency_claims.completed_at
`

type CompleteActorInputClaimParams struct {
	EnvironmentID      pgtype.UUID `json:"environment_id"`
	ClaimID            pgtype.UUID `json:"claim_id"`
	RequestFingerprint []byte      `json:"request_fingerprint"`
	SessionID          pgtype.UUID `json:"session_id"`
	RecordID           pgtype.UUID `json:"record_id"`
}

func (q *Queries) CompleteActorInputClaim(ctx context.Context, arg CompleteActorInputClaimParams) (IdempotencyClaim, error) {
	row := q.db.QueryRow(ctx, completeActorInputClaim,
		arg.EnvironmentID,
		arg.ClaimID,
		arg.RequestFingerprint,
		arg.SessionID,
		arg.RecordID,
	)
	var i IdempotencyClaim
	err := row.Scan(
		&i.ID,
		&i.EnvironmentID,
		&i.Operation,
		&i.SlotHash,
		&i.RequestFingerprint,
		&i.State,
		&i.Receipt,
		&i.AcceptedAt,
		&i.ExpiresAt,
		&i.RetiredAt,
		&i.CompletedAt,
	)
	return i, err
}

const completeActorOutputClaim = `-- name: CompleteActorOutputClaim :one
UPDATE idempotency_claims
   SET state = 'completed',
       receipt = jsonb_build_object(
	       'session_record_id', session_records.id::text,
           'sequence', session_records.sequence
       ),
       completed_at = transaction_timestamp()
  FROM session_records
 WHERE idempotency_claims.environment_id = $1::uuid
   AND idempotency_claims.id = $2
   AND idempotency_claims.operation = 'session.output.append'
   AND idempotency_claims.request_fingerprint = $3
   AND idempotency_claims.state = 'pending'
   AND idempotency_claims.retired_at IS NULL
   AND session_records.environment_id = idempotency_claims.environment_id
   AND session_records.session_id = $4
   AND session_records.id = $5
   AND session_records.direction = 'output'
   AND session_records.claim_id = idempotency_claims.id
RETURNING idempotency_claims.id, idempotency_claims.environment_id, idempotency_claims.operation, idempotency_claims.slot_hash, idempotency_claims.request_fingerprint, idempotency_claims.state, idempotency_claims.receipt, idempotency_claims.accepted_at, idempotency_claims.expires_at, idempotency_claims.retired_at, idempotency_claims.completed_at
`

type CompleteActorOutputClaimParams struct {
	EnvironmentID      pgtype.UUID `json:"environment_id"`
	ClaimID            pgtype.UUID `json:"claim_id"`
	RequestFingerprint []byte      `json:"request_fingerprint"`
	SessionID          pgtype.UUID `json:"session_id"`
	RecordID           pgtype.UUID `json:"record_id"`
}

func (q *Queries) CompleteActorOutputClaim(ctx context.Context, arg CompleteActorOutputClaimParams) (IdempotencyClaim, error) {
	row := q.db.QueryRow(ctx, completeActorOutputClaim,
		arg.EnvironmentID,
		arg.ClaimID,
		arg.RequestFingerprint,
		arg.SessionID,
		arg.RecordID,
	)
	var i IdempotencyClaim
	err := row.Scan(
		&i.ID,
		&i.EnvironmentID,
		&i.Operation,
		&i.SlotHash,
		&i.RequestFingerprint,
		&i.State,
		&i.Receipt,
		&i.AcceptedAt,
		&i.ExpiresAt,
		&i.RetiredAt,
		&i.CompletedAt,
	)
	return i, err
}

const createActorInputReconcileOutbox = `-- name: CreateActorInputReconcileOutbox :exec
INSERT INTO outbox_messages (id, lane, topic, partition_key, payload, available_at)
VALUES (
    $1,
    'control',
    'session.input.reconcile',
    $2::uuid::text,
    jsonb_build_object(
        'environmentId', $3::uuid::text,
        'sessionId', $2::uuid::text,
        'recordId', $4::uuid::text
    ),
    transaction_timestamp()
)
ON CONFLICT (id) DO NOTHING
`

type CreateActorInputReconcileOutboxParams struct {
	ID            pgtype.UUID `json:"id"`
	SessionID     pgtype.UUID `json:"session_id"`
	EnvironmentID pgtype.UUID `json:"environment_id"`
	RecordID      pgtype.UUID `json:"record_id"`
}

func (q *Queries) CreateActorInputReconcileOutbox(ctx context.Context, arg CreateActorInputReconcileOutboxParams) error {
	_, err := q.db.Exec(ctx, createActorInputReconcileOutbox,
		arg.ID,
		arg.SessionID,
		arg.EnvironmentID,
		arg.RecordID,
	)
	return err
}

const createActorStartInputRecord = `-- name: CreateActorStartInputRecord :one
WITH locked_actor AS MATERIALIZED (
    SELECT sessions.id, sessions.environment_id, sessions.actor_declared_id, sessions.deployment_definition_id, sessions.workspace_id, sessions.key, sessions.current_run_id, sessions.run_generation, sessions.state_version, sessions.manual_run_cancelled, sessions.failure, sessions.failure_run_id, sessions.next_input_sequence, sessions.committed_input_sequence, sessions.next_output_sequence, sessions.input_retention_floor, sessions.output_retention_floor, sessions.run_queue_name, sessions.run_concurrency_key, sessions.run_queue_concurrency_limit, sessions.run_priority, sessions.run_queue_ttl_ms, sessions.run_max_active_duration_ms, sessions.run_retry_policy, sessions.run_metadata, sessions.run_tags, sessions.state, sessions.close_sequence, sessions.created_at, sessions.updated_at, sessions.closed_at, sessions.cancelled_at, sessions.failed_at
      FROM sessions
     WHERE sessions.environment_id = $4
       AND sessions.id = $5
       AND sessions.state = 'open'
       AND sessions.next_input_sequence = 1
       AND sessions.committed_input_sequence = 0
       AND (
           $3::uuid IS NULL
           OR EXISTS (
               SELECT 1
                 FROM idempotency_claims
                WHERE idempotency_claims.environment_id = sessions.environment_id
                  AND idempotency_claims.id = $3
                  AND idempotency_claims.operation = 'actor.start'
                  AND idempotency_claims.state = 'pending'
                  AND idempotency_claims.retired_at IS NULL
           )
       )
     FOR UPDATE OF sessions
), advanced AS (
    UPDATE sessions
       SET next_input_sequence = 2,
           updated_at = now()
      FROM locked_actor
     WHERE sessions.id = locked_actor.id
    RETURNING sessions.id, sessions.environment_id, sessions.actor_declared_id, sessions.deployment_definition_id, sessions.workspace_id, sessions.key, sessions.current_run_id, sessions.run_generation, sessions.state_version, sessions.manual_run_cancelled, sessions.failure, sessions.failure_run_id, sessions.next_input_sequence, sessions.committed_input_sequence, sessions.next_output_sequence, sessions.input_retention_floor, sessions.output_retention_floor, sessions.run_queue_name, sessions.run_concurrency_key, sessions.run_queue_concurrency_limit, sessions.run_priority, sessions.run_queue_ttl_ms, sessions.run_max_active_duration_ms, sessions.run_retry_policy, sessions.run_metadata, sessions.run_tags, sessions.state, sessions.close_sequence, sessions.created_at, sessions.updated_at, sessions.closed_at, sessions.cancelled_at, sessions.failed_at
)
INSERT INTO session_records (
    id,
    environment_id,
    session_id,
    direction,
    sequence,
    data,
    content_type,
    source_kind,
    claim_id
)
SELECT $1,
       advanced.environment_id,
       advanced.id,
       'input',
       1,
       $2,
       'application/json',
       'external',
       $3
  FROM advanced
RETURNING id, environment_id, session_id, direction, sequence, data, content_type, source_kind, source_run_id, producer_run_id, producer_attempt_number, claim_id, created_at
`

type CreateActorStartInputRecordParams struct {
	ID            pgtype.UUID `json:"id"`
	Data          []byte      `json:"data"`
	ClaimID       pgtype.UUID `json:"claim_id"`
	EnvironmentID pgtype.UUID `json:"environment_id"`
	SessionID     pgtype.UUID `json:"session_id"`
}

func (q *Queries) CreateActorStartInputRecord(ctx context.Context, arg CreateActorStartInputRecordParams) (SessionRecord, error) {
	row := q.db.QueryRow(ctx, createActorStartInputRecord,
		arg.ID,
		arg.Data,
		arg.ClaimID,
		arg.EnvironmentID,
		arg.SessionID,
	)
	var i SessionRecord
	err := row.Scan(
		&i.ID,
		&i.EnvironmentID,
		&i.SessionID,
		&i.Direction,
		&i.Sequence,
		&i.Data,
		&i.ContentType,
		&i.SourceKind,
		&i.SourceRunID,
		&i.ProducerRunID,
		&i.ProducerAttemptNumber,
		&i.ClaimID,
		&i.CreatedAt,
	)
	return i, err
}

const getActorInputCurrentRun = `-- name: GetActorInputCurrentRun :one
SELECT runs.id, runs.org_id, runs.project_id, runs.environment_id, runs.deployment_id, runs.deployment_definition_id, runs.entrypoint_kind, runs.entrypoint_declared_id, runs.session_id, runs.cause_kind, runs.schedule_id, runs.schedule_generation, runs.scheduled_at, runs.previous_scheduled_at, runs.schedule_timezone, runs.parent_run_id, runs.parent_owns_lifecycle, runs.workspace_id, runs.base_workspace_version_id, runs.session_input_start_sequence, runs.session_input_high_watermark, runs.payload, runs.output, runs.failure, runs.status, runs.state_version, runs.current_attempt_number, runs.current_run_lease_id, runs.metadata, runs.tags, runs.queue_name, runs.concurrency_key, runs.queue_concurrency_limit, runs.priority, runs.queue_origin_at, runs.queue_score_at, runs.queued_expires_at, runs.max_active_duration_ms, runs.retry_policy, runs.active_elapsed_ms, runs.active_started_at, runs.trace_id, runs.root_span_id, runs.claim_id, runs.created_at, runs.updated_at, runs.first_lease_at, runs.started_at, runs.retry_at, runs.terminal_at
  FROM runs
 WHERE runs.environment_id = $1
   AND runs.id = $2
   AND runs.session_id = $3
`

type GetActorInputCurrentRunParams struct {
	EnvironmentID pgtype.UUID `json:"environment_id"`
	RunID         pgtype.UUID `json:"run_id"`
	SessionID     pgtype.UUID `json:"session_id"`
}

func (q *Queries) GetActorInputCurrentRun(ctx context.Context, arg GetActorInputCurrentRunParams) (Run, error) {
	row := q.db.QueryRow(ctx, getActorInputCurrentRun, arg.EnvironmentID, arg.RunID, arg.SessionID)
	var i Run
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.DeploymentID,
		&i.DeploymentDefinitionID,
		&i.EntrypointKind,
		&i.EntrypointDeclaredID,
		&i.SessionID,
		&i.CauseKind,
		&i.ScheduleID,
		&i.ScheduleGeneration,
		&i.ScheduledAt,
		&i.PreviousScheduledAt,
		&i.ScheduleTimezone,
		&i.ParentRunID,
		&i.ParentOwnsLifecycle,
		&i.WorkspaceID,
		&i.BaseWorkspaceVersionID,
		&i.SessionInputStartSequence,
		&i.SessionInputHighWatermark,
		&i.Payload,
		&i.Output,
		&i.Failure,
		&i.Status,
		&i.StateVersion,
		&i.CurrentAttemptNumber,
		&i.CurrentRunLeaseID,
		&i.Metadata,
		&i.Tags,
		&i.QueueName,
		&i.ConcurrencyKey,
		&i.QueueConcurrencyLimit,
		&i.Priority,
		&i.QueueOriginAt,
		&i.QueueScoreAt,
		&i.QueuedExpiresAt,
		&i.MaxActiveDurationMs,
		&i.RetryPolicy,
		&i.ActiveElapsedMs,
		&i.ActiveStartedAt,
		&i.TraceID,
		&i.RootSpanID,
		&i.ClaimID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.FirstLeaseAt,
		&i.StartedAt,
		&i.RetryAt,
		&i.TerminalAt,
	)
	return i, err
}

const getActorInputRecordAtSequenceForUpdate = `-- name: GetActorInputRecordAtSequenceForUpdate :one
SELECT id, environment_id, session_id, direction, sequence, data, content_type, source_kind, source_run_id, producer_run_id, producer_attempt_number, claim_id, created_at
  FROM session_records
 WHERE environment_id = $1
   AND session_id = $2
   AND direction = 'input'
   AND sequence = $3
 FOR UPDATE
`

type GetActorInputRecordAtSequenceForUpdateParams struct {
	EnvironmentID pgtype.UUID `json:"environment_id"`
	SessionID     pgtype.UUID `json:"session_id"`
	Sequence      int64       `json:"sequence"`
}

func (q *Queries) GetActorInputRecordAtSequenceForUpdate(ctx context.Context, arg GetActorInputRecordAtSequenceForUpdateParams) (SessionRecord, error) {
	row := q.db.QueryRow(ctx, getActorInputRecordAtSequenceForUpdate, arg.EnvironmentID, arg.SessionID, arg.Sequence)
	var i SessionRecord
	err := row.Scan(
		&i.ID,
		&i.EnvironmentID,
		&i.SessionID,
		&i.Direction,
		&i.Sequence,
		&i.Data,
		&i.ContentType,
		&i.SourceKind,
		&i.SourceRunID,
		&i.ProducerRunID,
		&i.ProducerAttemptNumber,
		&i.ClaimID,
		&i.CreatedAt,
	)
	return i, err
}

const getActorInputRecordByIDForUpdate = `-- name: GetActorInputRecordByIDForUpdate :one
SELECT id, environment_id, session_id, direction, sequence, data, content_type, source_kind, source_run_id, producer_run_id, producer_attempt_number, claim_id, created_at
  FROM session_records
 WHERE environment_id = $1
   AND session_id = $2
   AND id = $3
   AND direction = 'input'
 FOR UPDATE
`

type GetActorInputRecordByIDForUpdateParams struct {
	EnvironmentID pgtype.UUID `json:"environment_id"`
	SessionID     pgtype.UUID `json:"session_id"`
	ID            pgtype.UUID `json:"id"`
}

func (q *Queries) GetActorInputRecordByIDForUpdate(ctx context.Context, arg GetActorInputRecordByIDForUpdateParams) (SessionRecord, error) {
	row := q.db.QueryRow(ctx, getActorInputRecordByIDForUpdate, arg.EnvironmentID, arg.SessionID, arg.ID)
	var i SessionRecord
	err := row.Scan(
		&i.ID,
		&i.EnvironmentID,
		&i.SessionID,
		&i.Direction,
		&i.Sequence,
		&i.Data,
		&i.ContentType,
		&i.SourceKind,
		&i.SourceRunID,
		&i.ProducerRunID,
		&i.ProducerAttemptNumber,
		&i.ClaimID,
		&i.CreatedAt,
	)
	return i, err
}

const getActorOutputRecordByID = `-- name: GetActorOutputRecordByID :one
SELECT id, environment_id, session_id, direction, sequence, data, content_type, source_kind, source_run_id, producer_run_id, producer_attempt_number, claim_id, created_at
  FROM session_records
 WHERE environment_id = $1
   AND session_id = $2
   AND id = $3
   AND direction = 'output'
`

type GetActorOutputRecordByIDParams struct {
	EnvironmentID pgtype.UUID `json:"environment_id"`
	SessionID     pgtype.UUID `json:"session_id"`
	ID            pgtype.UUID `json:"id"`
}

func (q *Queries) GetActorOutputRecordByID(ctx context.Context, arg GetActorOutputRecordByIDParams) (SessionRecord, error) {
	row := q.db.QueryRow(ctx, getActorOutputRecordByID, arg.EnvironmentID, arg.SessionID, arg.ID)
	var i SessionRecord
	err := row.Scan(
		&i.ID,
		&i.EnvironmentID,
		&i.SessionID,
		&i.Direction,
		&i.Sequence,
		&i.Data,
		&i.ContentType,
		&i.SourceKind,
		&i.SourceRunID,
		&i.ProducerRunID,
		&i.ProducerAttemptNumber,
		&i.ClaimID,
		&i.CreatedAt,
	)
	return i, err
}

const getActorWakeupRecord = `-- name: GetActorWakeupRecord :one
SELECT session_records.id, session_records.environment_id, session_records.session_id, session_records.direction, session_records.sequence, session_records.data, session_records.content_type, session_records.source_kind, session_records.source_run_id, session_records.producer_run_id, session_records.producer_attempt_number, session_records.claim_id, session_records.created_at
  FROM sessions
  JOIN session_records
    ON session_records.session_id = sessions.id
   AND session_records.direction = 'input'
   AND session_records.sequence > sessions.committed_input_sequence
 WHERE sessions.environment_id = $1
   AND sessions.id = $2
   AND sessions.state = 'open'
 ORDER BY session_records.sequence, session_records.id
LIMIT 1
`

type GetActorWakeupRecordParams struct {
	EnvironmentID pgtype.UUID `json:"environment_id"`
	SessionID     pgtype.UUID `json:"session_id"`
}

func (q *Queries) GetActorWakeupRecord(ctx context.Context, arg GetActorWakeupRecordParams) (SessionRecord, error) {
	row := q.db.QueryRow(ctx, getActorWakeupRecord, arg.EnvironmentID, arg.SessionID)
	var i SessionRecord
	err := row.Scan(
		&i.ID,
		&i.EnvironmentID,
		&i.SessionID,
		&i.Direction,
		&i.Sequence,
		&i.Data,
		&i.ContentType,
		&i.SourceKind,
		&i.SourceRunID,
		&i.ProducerRunID,
		&i.ProducerAttemptNumber,
		&i.ClaimID,
		&i.CreatedAt,
	)
	return i, err
}

const listActorInputRecords = `-- name: ListActorInputRecords :many
SELECT id, environment_id, session_id, direction, sequence, data, content_type, source_kind, source_run_id, producer_run_id, producer_attempt_number, claim_id, created_at
  FROM session_records
 WHERE session_id = $1
   AND direction = 'input'
   AND sequence > $2
 ORDER BY sequence, id
 LIMIT $3
`

type ListActorInputRecordsParams struct {
	SessionID     pgtype.UUID `json:"session_id"`
	AfterSequence int64       `json:"after_sequence"`
	LimitCount    int32       `json:"limit_count"`
}

func (q *Queries) ListActorInputRecords(ctx context.Context, arg ListActorInputRecordsParams) ([]SessionRecord, error) {
	rows, err := q.db.Query(ctx, listActorInputRecords, arg.SessionID, arg.AfterSequence, arg.LimitCount)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SessionRecord
	for rows.Next() {
		var i SessionRecord
		if err := rows.Scan(
			&i.ID,
			&i.EnvironmentID,
			&i.SessionID,
			&i.Direction,
			&i.Sequence,
			&i.Data,
			&i.ContentType,
			&i.SourceKind,
			&i.SourceRunID,
			&i.ProducerRunID,
			&i.ProducerAttemptNumber,
			&i.ClaimID,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listActorOutputRecords = `-- name: ListActorOutputRecords :many
SELECT id, environment_id, session_id, direction, sequence, data, content_type, source_kind, source_run_id, producer_run_id, producer_attempt_number, claim_id, created_at
  FROM session_records
 WHERE session_id = $1
   AND direction = 'output'
   AND sequence > $2
 ORDER BY sequence, id
 LIMIT $3
`

type ListActorOutputRecordsParams struct {
	SessionID     pgtype.UUID `json:"session_id"`
	AfterSequence int64       `json:"after_sequence"`
	LimitCount    int32       `json:"limit_count"`
}

func (q *Queries) ListActorOutputRecords(ctx context.Context, arg ListActorOutputRecordsParams) ([]SessionRecord, error) {
	rows, err := q.db.Query(ctx, listActorOutputRecords, arg.SessionID, arg.AfterSequence, arg.LimitCount)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SessionRecord
	for rows.Next() {
		var i SessionRecord
		if err := rows.Scan(
			&i.ID,
			&i.EnvironmentID,
			&i.SessionID,
			&i.Direction,
			&i.Sequence,
			&i.Data,
			&i.ContentType,
			&i.SourceKind,
			&i.SourceRunID,
			&i.ProducerRunID,
			&i.ProducerAttemptNumber,
			&i.ClaimID,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const lockActorForInputReconcile = `-- name: LockActorForInputReconcile :one
SELECT id, environment_id, actor_declared_id, deployment_definition_id, workspace_id, key, current_run_id, run_generation, state_version, manual_run_cancelled, failure, failure_run_id, next_input_sequence, committed_input_sequence, next_output_sequence, input_retention_floor, output_retention_floor, run_queue_name, run_concurrency_key, run_queue_concurrency_limit, run_priority, run_queue_ttl_ms, run_max_active_duration_ms, run_retry_policy, run_metadata, run_tags, state, close_sequence, created_at, updated_at, closed_at, cancelled_at, failed_at
  FROM sessions
 WHERE environment_id = $1
   AND id = $2
 FOR UPDATE
`

type LockActorForInputReconcileParams struct {
	EnvironmentID pgtype.UUID `json:"environment_id"`
	SessionID     pgtype.UUID `json:"session_id"`
}

func (q *Queries) LockActorForInputReconcile(ctx context.Context, arg LockActorForInputReconcileParams) (Session, error) {
	row := q.db.QueryRow(ctx, lockActorForInputReconcile, arg.EnvironmentID, arg.SessionID)
	var i Session
	err := row.Scan(
		&i.ID,
		&i.EnvironmentID,
		&i.ActorDeclaredID,
		&i.DeploymentDefinitionID,
		&i.WorkspaceID,
		&i.Key,
		&i.CurrentRunID,
		&i.RunGeneration,
		&i.StateVersion,
		&i.ManualRunCancelled,
		&i.Failure,
		&i.FailureRunID,
		&i.NextInputSequence,
		&i.CommittedInputSequence,
		&i.NextOutputSequence,
		&i.InputRetentionFloor,
		&i.OutputRetentionFloor,
		&i.RunQueueName,
		&i.RunConcurrencyKey,
		&i.RunQueueConcurrencyLimit,
		&i.RunPriority,
		&i.RunQueueTtlMs,
		&i.RunMaxActiveDurationMs,
		&i.RunRetryPolicy,
		&i.RunMetadata,
		&i.RunTags,
		&i.State,
		&i.CloseSequence,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ClosedAt,
		&i.CancelledAt,
		&i.FailedAt,
	)
	return i, err
}

const lockActorInputClaim = `-- name: LockActorInputClaim :one
SELECT id, environment_id, operation, slot_hash, request_fingerprint, state, receipt, accepted_at, expires_at, retired_at, completed_at
  FROM idempotency_claims
 WHERE environment_id = $1
   AND id = $2
   AND operation = 'session.input.send'
   AND retired_at IS NULL
 FOR UPDATE
`

type LockActorInputClaimParams struct {
	EnvironmentID pgtype.UUID `json:"environment_id"`
	ID            pgtype.UUID `json:"id"`
}

func (q *Queries) LockActorInputClaim(ctx context.Context, arg LockActorInputClaimParams) (IdempotencyClaim, error) {
	row := q.db.QueryRow(ctx, lockActorInputClaim, arg.EnvironmentID, arg.ID)
	var i IdempotencyClaim
	err := row.Scan(
		&i.ID,
		&i.EnvironmentID,
		&i.Operation,
		&i.SlotHash,
		&i.RequestFingerprint,
		&i.State,
		&i.Receipt,
		&i.AcceptedAt,
		&i.ExpiresAt,
		&i.RetiredAt,
		&i.CompletedAt,
	)
	return i, err
}

const lockActorInputCurrentRun = `-- name: LockActorInputCurrentRun :one
SELECT runs.id, runs.org_id, runs.project_id, runs.environment_id, runs.deployment_id, runs.deployment_definition_id, runs.entrypoint_kind, runs.entrypoint_declared_id, runs.session_id, runs.cause_kind, runs.schedule_id, runs.schedule_generation, runs.scheduled_at, runs.previous_scheduled_at, runs.schedule_timezone, runs.parent_run_id, runs.parent_owns_lifecycle, runs.workspace_id, runs.base_workspace_version_id, runs.session_input_start_sequence, runs.session_input_high_watermark, runs.payload, runs.output, runs.failure, runs.status, runs.state_version, runs.current_attempt_number, runs.current_run_lease_id, runs.metadata, runs.tags, runs.queue_name, runs.concurrency_key, runs.queue_concurrency_limit, runs.priority, runs.queue_origin_at, runs.queue_score_at, runs.queued_expires_at, runs.max_active_duration_ms, runs.retry_policy, runs.active_elapsed_ms, runs.active_started_at, runs.trace_id, runs.root_span_id, runs.claim_id, runs.created_at, runs.updated_at, runs.first_lease_at, runs.started_at, runs.retry_at, runs.terminal_at
  FROM runs
 WHERE runs.environment_id = $1
   AND runs.id = $2
   AND runs.session_id = $3
 FOR UPDATE
`

type LockActorInputCurrentRunParams struct {
	EnvironmentID pgtype.UUID `json:"environment_id"`
	RunID         pgtype.UUID `json:"run_id"`
	SessionID     pgtype.UUID `json:"session_id"`
}

func (q *Queries) LockActorInputCurrentRun(ctx context.Context, arg LockActorInputCurrentRunParams) (Run, error) {
	row := q.db.QueryRow(ctx, lockActorInputCurrentRun, arg.EnvironmentID, arg.RunID, arg.SessionID)
	var i Run
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.DeploymentID,
		&i.DeploymentDefinitionID,
		&i.EntrypointKind,
		&i.EntrypointDeclaredID,
		&i.SessionID,
		&i.CauseKind,
		&i.ScheduleID,
		&i.ScheduleGeneration,
		&i.ScheduledAt,
		&i.PreviousScheduledAt,
		&i.ScheduleTimezone,
		&i.ParentRunID,
		&i.ParentOwnsLifecycle,
		&i.WorkspaceID,
		&i.BaseWorkspaceVersionID,
		&i.SessionInputStartSequence,
		&i.SessionInputHighWatermark,
		&i.Payload,
		&i.Output,
		&i.Failure,
		&i.Status,
		&i.StateVersion,
		&i.CurrentAttemptNumber,
		&i.CurrentRunLeaseID,
		&i.Metadata,
		&i.Tags,
		&i.QueueName,
		&i.ConcurrencyKey,
		&i.QueueConcurrencyLimit,
		&i.Priority,
		&i.QueueOriginAt,
		&i.QueueScoreAt,
		&i.QueuedExpiresAt,
		&i.MaxActiveDurationMs,
		&i.RetryPolicy,
		&i.ActiveElapsedMs,
		&i.ActiveStartedAt,
		&i.TraceID,
		&i.RootSpanID,
		&i.ClaimID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.FirstLeaseAt,
		&i.StartedAt,
		&i.RetryAt,
		&i.TerminalAt,
	)
	return i, err
}

const readPublicActorOutputPage = `-- name: ReadPublicActorOutputPage :many
WITH scoped_actor AS MATERIALIZED (
    SELECT sessions.id,
           sessions.output_retention_floor,
           sessions.next_output_sequence,
           CASE
               WHEN $2::boolean
               THEN $3::bigint
               ELSE sessions.output_retention_floor - 1
           END::bigint AS effective_after
     FROM sessions
     WHERE sessions.environment_id = $4
       AND sessions.id = $5
)
SELECT scoped_actor.id AS session_id,
       scoped_actor.output_retention_floor,
       scoped_actor.next_output_sequence,
       scoped_actor.effective_after::bigint AS effective_after,
       page.record_id,
       coalesce(page.sequence, 0)::bigint AS sequence,
       coalesce(page.data, 'null'::jsonb)::jsonb AS data,
       coalesce(page.content_type, '')::text AS content_type,
       coalesce(page.created_at, 'epoch'::timestamptz)::timestamptz AS created_at,
       page.run_id,
       coalesce(page.producer_attempt_number, 0)::integer AS producer_attempt_number,
       page.deployment_id
  FROM scoped_actor
  LEFT JOIN LATERAL (
      SELECT session_records.id AS record_id,
             session_records.sequence,
             session_records.data,
             session_records.content_type,
             session_records.created_at,
             runs.id AS run_id,
             session_records.producer_attempt_number,
             deployments.id AS deployment_id
        FROM session_records
        JOIN runs
          ON runs.session_id = session_records.session_id
         AND runs.id = session_records.producer_run_id
        JOIN deployments
          ON deployments.environment_id = runs.environment_id
         AND deployments.id = runs.deployment_id
       WHERE session_records.session_id = scoped_actor.id
         AND session_records.direction = 'output'
         AND session_records.sequence > scoped_actor.effective_after
         AND session_records.sequence < scoped_actor.next_output_sequence
       ORDER BY session_records.sequence, session_records.id
       LIMIT $1::integer
  ) AS page ON true
 ORDER BY page.sequence NULLS LAST, page.record_id NULLS LAST
`

type ReadPublicActorOutputPageParams struct {
	LimitCount    int32       `json:"limit_count"`
	AfterPresent  bool        `json:"after_present"`
	AfterSequence int64       `json:"after_sequence"`
	EnvironmentID pgtype.UUID `json:"environment_id"`
	SessionID     pgtype.UUID `json:"session_id"`
}

type ReadPublicActorOutputPageRow struct {
	SessionID             pgtype.UUID        `json:"session_id"`
	OutputRetentionFloor  int64              `json:"output_retention_floor"`
	NextOutputSequence    int64              `json:"next_output_sequence"`
	EffectiveAfter        int64              `json:"effective_after"`
	RecordID              pgtype.UUID        `json:"record_id"`
	Sequence              int64              `json:"sequence"`
	Data                  []byte             `json:"data"`
	ContentType           string             `json:"content_type"`
	CreatedAt             pgtype.Timestamptz `json:"created_at"`
	RunID                 pgtype.UUID        `json:"run_id"`
	ProducerAttemptNumber int32              `json:"producer_attempt_number"`
	DeploymentID          pgtype.UUID        `json:"deployment_id"`
}

func (q *Queries) ReadPublicActorOutputPage(ctx context.Context, arg ReadPublicActorOutputPageParams) ([]ReadPublicActorOutputPageRow, error) {
	rows, err := q.db.Query(ctx, readPublicActorOutputPage,
		arg.LimitCount,
		arg.AfterPresent,
		arg.AfterSequence,
		arg.EnvironmentID,
		arg.SessionID,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ReadPublicActorOutputPageRow
	for rows.Next() {
		var i ReadPublicActorOutputPageRow
		if err := rows.Scan(
			&i.SessionID,
			&i.OutputRetentionFloor,
			&i.NextOutputSequence,
			&i.EffectiveAfter,
			&i.RecordID,
			&i.Sequence,
			&i.Data,
			&i.ContentType,
			&i.CreatedAt,
			&i.RunID,
			&i.ProducerAttemptNumber,
			&i.DeploymentID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
