// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: runtime_checkpoints.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createReadyRuntimeCheckpointForRunWait = `-- name: CreateReadyRuntimeCheckpointForRunWait :one
WITH wait_scope AS (
    SELECT run_waits.id, run_waits.org_id, run_waits.project_id, run_waits.environment_id, run_waits.run_id, run_waits.kind, run_waits.correlation_id, run_waits.state, run_waits.timeout_at, run_waits.runtime_checkpoint_id, run_waits.workspace_version_id, run_waits.active_elapsed_ms_at_park, run_waits.parked_at, run_waits.resolved_at, run_waits.resumed_at, run_waits.cancelled_at, run_waits.created_at, run_waits.updated_at,
           runs.workspace_id,
           runs.current_run_lease_id,
           runs.active_started_at,
           runs.active_elapsed_ms,
           runs.max_active_duration_ms,
           workspace_leases.id AS workspace_lease_id,
           workspace_leases.materialization_id
      FROM run_waits
      JOIN runs ON runs.org_id = run_waits.org_id
               AND runs.project_id = run_waits.project_id
               AND runs.environment_id = run_waits.environment_id
               AND runs.id = run_waits.run_id
      JOIN run_leases ON run_leases.org_id = runs.org_id
                     AND run_leases.run_id = runs.id
                     AND run_leases.id = runs.current_run_lease_id
      JOIN workspaces ON workspaces.org_id = runs.org_id
                     AND workspaces.project_id = runs.project_id
                     AND workspaces.environment_id = runs.environment_id
                     AND workspaces.id = runs.workspace_id
      JOIN workspace_versions ON workspace_versions.org_id = workspaces.org_id
                             AND workspace_versions.project_id = workspaces.project_id
                             AND workspace_versions.environment_id = workspaces.environment_id
                             AND workspace_versions.workspace_id = workspaces.id
                             AND workspace_versions.id = run_waits.workspace_version_id
                             AND workspace_versions.state = 'ready'
      JOIN workspace_leases ON workspace_leases.org_id = runs.org_id
                           AND workspace_leases.project_id = runs.project_id
                           AND workspace_leases.environment_id = runs.environment_id
                           AND workspace_leases.workspace_id = runs.workspace_id
                           AND workspace_leases.owner_run_id = runs.id
                           AND workspace_leases.lease_kind = 'write'
                           AND workspace_leases.state = 'active'
                           AND workspace_leases.released_at IS NULL
                           AND workspace_leases.expires_at > now()
      JOIN workspace_materializations ON workspace_materializations.org_id = workspace_leases.org_id
                                     AND workspace_materializations.project_id = workspace_leases.project_id
                                     AND workspace_materializations.environment_id = workspace_leases.environment_id
                                     AND workspace_materializations.workspace_id = workspace_leases.workspace_id
                                     AND workspace_materializations.id = workspace_leases.materialization_id
     WHERE run_waits.org_id = $1
       AND run_waits.project_id = $2
       AND run_waits.environment_id = $3
       AND run_waits.id = $4
       AND run_waits.run_id = $5
       AND run_waits.state = 'parking'
       AND runs.status = 'running'
       AND runs.current_run_lease_id = $6
       AND run_leases.worker_instance_id = $7
       AND run_leases.status IN ('leased', 'running')
       AND run_leases.lease_expires_at > now()
       AND run_waits.workspace_version_id IS NOT NULL
       AND workspaces.current_version_id = run_waits.workspace_version_id
       AND workspace_materializations.dirty_generation = 0
       AND workspace_materializations.state IN ('running', 'pausing', 'paused')
       AND NOT EXISTS (
           SELECT 1
             FROM workspace_leases AS other_workspace_leases
            WHERE other_workspace_leases.org_id = workspace_materializations.org_id
              AND other_workspace_leases.project_id = workspace_materializations.project_id
              AND other_workspace_leases.environment_id = workspace_materializations.environment_id
              AND other_workspace_leases.workspace_id = workspace_materializations.workspace_id
              AND other_workspace_leases.materialization_id = workspace_materializations.id
              AND other_workspace_leases.id <> workspace_leases.id
              AND other_workspace_leases.state IN ('active', 'releasing')
              AND other_workspace_leases.expires_at > now()
       )
     FOR UPDATE OF run_waits, runs, workspace_leases, workspace_materializations
),
created_checkpoint AS (
    INSERT INTO runtime_checkpoints (
        id,
        org_id,
        project_id,
        environment_id,
        workspace_id,
        run_id,
        source_workspace_lease_id,
        materialization_id,
        base_workspace_version_id,
        state,
        runtime_backend,
        runtime_id,
        runtime_arch,
        runtime_abi,
        kernel_digest,
        initramfs_digest,
        rootfs_digest,
        runtime_config_digest,
        runtime_vcpus,
        runtime_memory_mib,
        runtime_scratch_disk_mib,
        cni_profile,
        image_key,
        manifest,
        expires_at,
        ready_at
    )
    SELECT $8,
           wait_scope.org_id,
           wait_scope.project_id,
           wait_scope.environment_id,
           wait_scope.workspace_id,
           wait_scope.run_id,
           wait_scope.workspace_lease_id,
           wait_scope.materialization_id,
           wait_scope.workspace_version_id,
           'ready',
           $9,
           $10,
           $11,
           $12,
           $13,
           $14,
           $15,
           $16,
           $17::int,
           $18::int,
           $19::int,
           $20,
           $21::text,
           $22::jsonb,
           CASE
             WHEN wait_scope.timeout_at IS NULL THEN NULL::timestamptz
             ELSE wait_scope.timeout_at + interval '1 day'
           END,
           now()
      FROM wait_scope
    RETURNING id, org_id, project_id, environment_id, workspace_id, run_id, source_workspace_lease_id, materialization_id, base_workspace_version_id, state, runtime_backend, runtime_id, runtime_arch, runtime_abi, kernel_digest, initramfs_digest, rootfs_digest, runtime_config_digest, runtime_vcpus, runtime_memory_mib, runtime_scratch_disk_mib, cni_profile, image_key, manifest, error_message, expires_at, created_at, ready_at, restoring_at, invalidated_at
),
updated_wait AS (
    UPDATE run_waits
       SET state = 'waiting',
           runtime_checkpoint_id = created_checkpoint.id,
           workspace_version_id = created_checkpoint.base_workspace_version_id,
           active_elapsed_ms_at_park = LEAST(
               wait_scope.max_active_duration_ms,
               wait_scope.active_elapsed_ms
               + CASE
                   WHEN wait_scope.active_started_at IS NULL THEN 0
                   ELSE GREATEST((EXTRACT(EPOCH FROM (now() - wait_scope.active_started_at)) * 1000)::bigint, 0)
                 END
           ),
           updated_at = now()
      FROM wait_scope, created_checkpoint
     WHERE run_waits.org_id = wait_scope.org_id
       AND run_waits.id = wait_scope.id
       AND run_waits.state = 'parking'
    RETURNING run_waits.id, run_waits.org_id, run_waits.project_id, run_waits.environment_id, run_waits.run_id, run_waits.kind, run_waits.correlation_id, run_waits.state, run_waits.timeout_at, run_waits.runtime_checkpoint_id, run_waits.workspace_version_id, run_waits.active_elapsed_ms_at_park, run_waits.parked_at, run_waits.resolved_at, run_waits.resumed_at, run_waits.cancelled_at, run_waits.created_at, run_waits.updated_at
),
released_workspace_lease AS (
    UPDATE workspace_leases
       SET state = 'released',
           released_at = now(),
           updated_at = now()
      FROM wait_scope, updated_wait
     WHERE workspace_leases.org_id = wait_scope.org_id
       AND workspace_leases.id = wait_scope.workspace_lease_id
       AND workspace_leases.state = 'active'
    RETURNING workspace_leases.id
),
stopped_materialization AS (
    UPDATE workspace_materializations
       SET state = 'stopped',
           stopped_at = coalesce(workspace_materializations.stopped_at, now()),
           reservation_expires_at = NULL,
           reserved_cpu_millis = 0,
           reserved_memory_mib = 0,
           reserved_disk_mib = 0,
           reserved_execution_slots = 0,
           capacity_reservation_id = NULL,
           updated_at = now()
      FROM wait_scope, released_workspace_lease
     WHERE workspace_materializations.org_id = wait_scope.org_id
       AND workspace_materializations.project_id = wait_scope.project_id
       AND workspace_materializations.environment_id = wait_scope.environment_id
       AND workspace_materializations.workspace_id = wait_scope.workspace_id
       AND workspace_materializations.id = wait_scope.materialization_id
       AND workspace_materializations.state IN ('running', 'pausing', 'paused')
    RETURNING workspace_materializations.id
),
detached_run_lease AS (
    UPDATE run_leases
       SET status = 'detached',
           active_duration_ms = updated_wait.active_elapsed_ms_at_park,
           renewed_at = now()
      FROM wait_scope, updated_wait
     WHERE run_leases.org_id = wait_scope.org_id
       AND run_leases.run_id = wait_scope.run_id
       AND run_leases.id = wait_scope.current_run_lease_id
       AND run_leases.worker_instance_id = $7
       AND run_leases.status IN ('leased', 'running')
    RETURNING run_leases.id
),
parked_run AS (
    UPDATE runs
       SET status = 'waiting',
           execution_status = 'waiting',
           current_run_lease_id = NULL,
           latest_runtime_checkpoint_id = created_checkpoint.id,
           active_elapsed_ms = updated_wait.active_elapsed_ms_at_park,
           active_started_at = NULL,
           state_version = runs.state_version + 1,
           updated_at = now()
      FROM wait_scope, created_checkpoint, updated_wait
     WHERE runs.org_id = wait_scope.org_id
       AND runs.id = wait_scope.run_id
       AND runs.status = 'running'
    RETURNING runs.id
),
parked_attempt AS (
    UPDATE run_attempts
       SET status = 'waiting',
           updated_at = now()
      FROM wait_scope, parked_run
     WHERE run_attempts.org_id = wait_scope.org_id
       AND run_attempts.run_id = wait_scope.run_id
       AND run_attempts.id = (
           SELECT runs.current_attempt_id
             FROM runs
            WHERE runs.org_id = wait_scope.org_id
              AND runs.id = wait_scope.run_id
       )
       AND run_attempts.status = 'running'
    RETURNING run_attempts.run_id
),
parked_queue AS (
    UPDATE run_queue_items
       SET status = 'parked',
           dispatch_message_id = NULL,
           reserved_by_worker_instance_id = NULL,
           reservation_expires_at = NULL,
           updated_at = now()
      FROM wait_scope, parked_run
     WHERE run_queue_items.org_id = wait_scope.org_id
       AND run_queue_items.run_id = wait_scope.run_id
       AND run_queue_items.status IN ('reserved', 'published')
    RETURNING run_queue_items.run_id
)
SELECT created_checkpoint.id, created_checkpoint.org_id, created_checkpoint.project_id, created_checkpoint.environment_id, created_checkpoint.workspace_id, created_checkpoint.run_id, created_checkpoint.source_workspace_lease_id, created_checkpoint.materialization_id, created_checkpoint.base_workspace_version_id, created_checkpoint.state, created_checkpoint.runtime_backend, created_checkpoint.runtime_id, created_checkpoint.runtime_arch, created_checkpoint.runtime_abi, created_checkpoint.kernel_digest, created_checkpoint.initramfs_digest, created_checkpoint.rootfs_digest, created_checkpoint.runtime_config_digest, created_checkpoint.runtime_vcpus, created_checkpoint.runtime_memory_mib, created_checkpoint.runtime_scratch_disk_mib, created_checkpoint.cni_profile, created_checkpoint.image_key, created_checkpoint.manifest, created_checkpoint.error_message, created_checkpoint.expires_at, created_checkpoint.created_at, created_checkpoint.ready_at, created_checkpoint.restoring_at, created_checkpoint.invalidated_at
  FROM created_checkpoint
  JOIN updated_wait ON true
  JOIN released_workspace_lease ON true
  JOIN stopped_materialization ON true
  JOIN detached_run_lease ON true
  JOIN parked_run ON true
  JOIN parked_attempt ON true
`

type CreateReadyRuntimeCheckpointForRunWaitParams struct {
	OrgID                 pgtype.UUID `json:"org_id"`
	ProjectID             pgtype.UUID `json:"project_id"`
	EnvironmentID         pgtype.UUID `json:"environment_id"`
	RunWaitID             pgtype.UUID `json:"run_wait_id"`
	RunID                 pgtype.UUID `json:"run_id"`
	RunLeaseID            pgtype.UUID `json:"run_lease_id"`
	WorkerInstanceID      pgtype.UUID `json:"worker_instance_id"`
	RuntimeCheckpointID   pgtype.UUID `json:"runtime_checkpoint_id"`
	RuntimeBackend        string      `json:"runtime_backend"`
	RuntimeID             string      `json:"runtime_id"`
	RuntimeArch           string      `json:"runtime_arch"`
	RuntimeABI            string      `json:"runtime_abi"`
	KernelDigest          string      `json:"kernel_digest"`
	InitramfsDigest       string      `json:"initramfs_digest"`
	RootfsDigest          string      `json:"rootfs_digest"`
	RuntimeConfigDigest   string      `json:"runtime_config_digest"`
	RuntimeVcpus          pgtype.Int4 `json:"runtime_vcpus"`
	RuntimeMemoryMib      pgtype.Int4 `json:"runtime_memory_mib"`
	RuntimeScratchDiskMib pgtype.Int4 `json:"runtime_scratch_disk_mib"`
	CniProfile            string      `json:"cni_profile"`
	ImageKey              pgtype.Text `json:"image_key"`
	Manifest              []byte      `json:"manifest"`
}

type CreateReadyRuntimeCheckpointForRunWaitRow struct {
	ID                     pgtype.UUID            `json:"id"`
	OrgID                  pgtype.UUID            `json:"org_id"`
	ProjectID              pgtype.UUID            `json:"project_id"`
	EnvironmentID          pgtype.UUID            `json:"environment_id"`
	WorkspaceID            pgtype.UUID            `json:"workspace_id"`
	RunID                  pgtype.UUID            `json:"run_id"`
	SourceWorkspaceLeaseID pgtype.UUID            `json:"source_workspace_lease_id"`
	MaterializationID      pgtype.UUID            `json:"materialization_id"`
	BaseWorkspaceVersionID pgtype.UUID            `json:"base_workspace_version_id"`
	State                  RuntimeCheckpointState `json:"state"`
	RuntimeBackend         string                 `json:"runtime_backend"`
	RuntimeID              string                 `json:"runtime_id"`
	RuntimeArch            string                 `json:"runtime_arch"`
	RuntimeABI             string                 `json:"runtime_abi"`
	KernelDigest           string                 `json:"kernel_digest"`
	InitramfsDigest        string                 `json:"initramfs_digest"`
	RootfsDigest           string                 `json:"rootfs_digest"`
	RuntimeConfigDigest    string                 `json:"runtime_config_digest"`
	RuntimeVcpus           pgtype.Int4            `json:"runtime_vcpus"`
	RuntimeMemoryMib       pgtype.Int4            `json:"runtime_memory_mib"`
	RuntimeScratchDiskMib  pgtype.Int4            `json:"runtime_scratch_disk_mib"`
	CniProfile             string                 `json:"cni_profile"`
	ImageKey               pgtype.Text            `json:"image_key"`
	Manifest               []byte                 `json:"manifest"`
	ErrorMessage           pgtype.Text            `json:"error_message"`
	ExpiresAt              pgtype.Timestamptz     `json:"expires_at"`
	CreatedAt              pgtype.Timestamptz     `json:"created_at"`
	ReadyAt                pgtype.Timestamptz     `json:"ready_at"`
	RestoringAt            pgtype.Timestamptz     `json:"restoring_at"`
	InvalidatedAt          pgtype.Timestamptz     `json:"invalidated_at"`
}

func (q *Queries) CreateReadyRuntimeCheckpointForRunWait(ctx context.Context, arg CreateReadyRuntimeCheckpointForRunWaitParams) (CreateReadyRuntimeCheckpointForRunWaitRow, error) {
	row := q.db.QueryRow(ctx, createReadyRuntimeCheckpointForRunWait,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.RunWaitID,
		arg.RunID,
		arg.RunLeaseID,
		arg.WorkerInstanceID,
		arg.RuntimeCheckpointID,
		arg.RuntimeBackend,
		arg.RuntimeID,
		arg.RuntimeArch,
		arg.RuntimeABI,
		arg.KernelDigest,
		arg.InitramfsDigest,
		arg.RootfsDigest,
		arg.RuntimeConfigDigest,
		arg.RuntimeVcpus,
		arg.RuntimeMemoryMib,
		arg.RuntimeScratchDiskMib,
		arg.CniProfile,
		arg.ImageKey,
		arg.Manifest,
	)
	var i CreateReadyRuntimeCheckpointForRunWaitRow
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.WorkspaceID,
		&i.RunID,
		&i.SourceWorkspaceLeaseID,
		&i.MaterializationID,
		&i.BaseWorkspaceVersionID,
		&i.State,
		&i.RuntimeBackend,
		&i.RuntimeID,
		&i.RuntimeArch,
		&i.RuntimeABI,
		&i.KernelDigest,
		&i.InitramfsDigest,
		&i.RootfsDigest,
		&i.RuntimeConfigDigest,
		&i.RuntimeVcpus,
		&i.RuntimeMemoryMib,
		&i.RuntimeScratchDiskMib,
		&i.CniProfile,
		&i.ImageKey,
		&i.Manifest,
		&i.ErrorMessage,
		&i.ExpiresAt,
		&i.CreatedAt,
		&i.ReadyAt,
		&i.RestoringAt,
		&i.InvalidatedAt,
	)
	return i, err
}

const createRuntimeCheckpointArtifact = `-- name: CreateRuntimeCheckpointArtifact :one
INSERT INTO runtime_checkpoint_artifacts (
    org_id,
    project_id,
    environment_id,
    run_id,
    runtime_checkpoint_id,
    role,
    ordinal,
    artifact_id,
    size_bytes,
    media_type,
    digest,
    encrypt_duration_ms,
    store_duration_ms
)
SELECT runtime_checkpoints.org_id,
       runtime_checkpoints.project_id,
       runtime_checkpoints.environment_id,
       runtime_checkpoints.run_id,
       runtime_checkpoints.id,
       $1::runtime_checkpoint_artifact_role,
       $2::int,
       artifacts.id,
       artifacts.size_bytes,
       artifacts.media_type,
       artifacts.digest,
       $3::bigint,
       $4::bigint
  FROM runtime_checkpoints
  JOIN artifacts ON artifacts.org_id = runtime_checkpoints.org_id
                AND artifacts.project_id = runtime_checkpoints.project_id
                AND artifacts.environment_id = runtime_checkpoints.environment_id
                AND artifacts.id = $5
                AND artifacts.digest = $6
 WHERE runtime_checkpoints.org_id = $7
   AND runtime_checkpoints.project_id = $8
   AND runtime_checkpoints.environment_id = $9
   AND runtime_checkpoints.run_id = $10
   AND runtime_checkpoints.id = $11
RETURNING org_id, project_id, environment_id, run_id, runtime_checkpoint_id, role, ordinal, artifact_id, size_bytes, media_type, digest, encrypt_duration_ms, store_duration_ms, created_at
`

type CreateRuntimeCheckpointArtifactParams struct {
	Role                RuntimeCheckpointArtifactRole `json:"role"`
	Ordinal             int32                         `json:"ordinal"`
	EncryptDurationMs   int64                         `json:"encrypt_duration_ms"`
	StoreDurationMs     int64                         `json:"store_duration_ms"`
	ArtifactID          pgtype.UUID                   `json:"artifact_id"`
	Digest              string                        `json:"digest"`
	OrgID               pgtype.UUID                   `json:"org_id"`
	ProjectID           pgtype.UUID                   `json:"project_id"`
	EnvironmentID       pgtype.UUID                   `json:"environment_id"`
	RunID               pgtype.UUID                   `json:"run_id"`
	RuntimeCheckpointID pgtype.UUID                   `json:"runtime_checkpoint_id"`
}

func (q *Queries) CreateRuntimeCheckpointArtifact(ctx context.Context, arg CreateRuntimeCheckpointArtifactParams) (RuntimeCheckpointArtifact, error) {
	row := q.db.QueryRow(ctx, createRuntimeCheckpointArtifact,
		arg.Role,
		arg.Ordinal,
		arg.EncryptDurationMs,
		arg.StoreDurationMs,
		arg.ArtifactID,
		arg.Digest,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.RunID,
		arg.RuntimeCheckpointID,
	)
	var i RuntimeCheckpointArtifact
	err := row.Scan(
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.RunID,
		&i.RuntimeCheckpointID,
		&i.Role,
		&i.Ordinal,
		&i.ArtifactID,
		&i.SizeBytes,
		&i.MediaType,
		&i.Digest,
		&i.EncryptDurationMs,
		&i.StoreDurationMs,
		&i.CreatedAt,
	)
	return i, err
}

const failParkingRunWait = `-- name: FailParkingRunWait :one
UPDATE run_waits
   SET state = 'failed',
       updated_at = now()
 WHERE org_id = $1
   AND project_id = $2
   AND environment_id = $3
   AND run_id = $4
   AND id = $5
   AND (
       runtime_checkpoint_id IS NULL
       OR runtime_checkpoint_id = $6
   )
   AND state = 'parking'
RETURNING id, org_id, project_id, environment_id, run_id, kind, correlation_id, state, timeout_at, runtime_checkpoint_id, workspace_version_id, active_elapsed_ms_at_park, parked_at, resolved_at, resumed_at, cancelled_at, created_at, updated_at
`

type FailParkingRunWaitParams struct {
	OrgID               pgtype.UUID `json:"org_id"`
	ProjectID           pgtype.UUID `json:"project_id"`
	EnvironmentID       pgtype.UUID `json:"environment_id"`
	RunID               pgtype.UUID `json:"run_id"`
	RunWaitID           pgtype.UUID `json:"run_wait_id"`
	RuntimeCheckpointID pgtype.UUID `json:"runtime_checkpoint_id"`
}

func (q *Queries) FailParkingRunWait(ctx context.Context, arg FailParkingRunWaitParams) (RunWait, error) {
	row := q.db.QueryRow(ctx, failParkingRunWait,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.RunID,
		arg.RunWaitID,
		arg.RuntimeCheckpointID,
	)
	var i RunWait
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.RunID,
		&i.Kind,
		&i.CorrelationID,
		&i.State,
		&i.TimeoutAt,
		&i.RuntimeCheckpointID,
		&i.WorkspaceVersionID,
		&i.ActiveElapsedMsAtPark,
		&i.ParkedAt,
		&i.ResolvedAt,
		&i.ResumedAt,
		&i.CancelledAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getRunRestorePayload = `-- name: GetRunRestorePayload :one
SELECT
    runtime_checkpoints.id AS runtime_checkpoint_id,
    runtime_checkpoints.manifest,
    run_waits.id AS run_wait_id,
    run_waits.correlation_id AS run_wait_correlation_id,
    run_waits.kind AS run_wait_kind,
    run_waits.state AS run_wait_state,
    streams.name AS stream_name,
    matched_stream_record.sequence AS stream_record_sequence,
    matched_stream_record.data AS stream_record_data,
    tokens.state AS token_state,
    tokens.completion_data AS token_completion_data,
    timer_waits.fire_at AS timer_fire_at
  FROM runs
  JOIN run_leases ON run_leases.org_id = runs.org_id
                      AND run_leases.run_id = runs.id
                      AND run_leases.id = runs.current_run_lease_id
                      AND run_leases.restore_runtime_checkpoint_id = runs.latest_runtime_checkpoint_id
  JOIN runtime_checkpoints ON runtime_checkpoints.org_id = runs.org_id
                  AND runtime_checkpoints.run_id = runs.id
                  AND runtime_checkpoints.id = runs.latest_runtime_checkpoint_id
  JOIN run_waits ON run_waits.org_id = runs.org_id
                AND run_waits.project_id = runs.project_id
                AND run_waits.environment_id = runs.environment_id
                AND run_waits.run_id = runs.id
                AND run_waits.runtime_checkpoint_id = runtime_checkpoints.id
                AND run_waits.state = 'resuming'
  LEFT JOIN stream_waits ON stream_waits.org_id = run_waits.org_id
                        AND stream_waits.project_id = run_waits.project_id
                        AND stream_waits.environment_id = run_waits.environment_id
                        AND stream_waits.run_wait_id = run_waits.id
  LEFT JOIN streams ON streams.org_id = stream_waits.org_id
                   AND streams.project_id = stream_waits.project_id
                   AND streams.environment_id = stream_waits.environment_id
                   AND streams.id = stream_waits.stream_id
  LEFT JOIN stream_records AS matched_stream_record
         ON matched_stream_record.org_id = stream_waits.org_id
        AND matched_stream_record.stream_id = stream_waits.stream_id
        AND matched_stream_record.id = stream_waits.matched_record_id
  LEFT JOIN token_waits ON token_waits.org_id = run_waits.org_id
                       AND token_waits.project_id = run_waits.project_id
                       AND token_waits.environment_id = run_waits.environment_id
                       AND token_waits.run_wait_id = run_waits.id
  LEFT JOIN tokens ON tokens.org_id = token_waits.org_id
                  AND tokens.project_id = token_waits.project_id
                  AND tokens.environment_id = token_waits.environment_id
                  AND tokens.id = token_waits.token_id
  LEFT JOIN timer_waits ON timer_waits.org_id = run_waits.org_id
                       AND timer_waits.project_id = run_waits.project_id
                       AND timer_waits.environment_id = run_waits.environment_id
                       AND timer_waits.run_wait_id = run_waits.id
 WHERE runs.org_id = $1
   AND runs.id = $2
   AND runs.current_run_lease_id = $3
   AND run_leases.worker_instance_id = $4
   AND run_leases.status IN ('leased', 'running')
   AND run_leases.lease_expires_at > now()
   AND runs.latest_runtime_checkpoint_id IS NOT NULL
   AND runtime_checkpoints.state = 'restoring'
   AND (runtime_checkpoints.expires_at IS NULL OR runtime_checkpoints.expires_at > now())
 LIMIT 1
`

type GetRunRestorePayloadParams struct {
	OrgID            pgtype.UUID `json:"org_id"`
	RunID            pgtype.UUID `json:"run_id"`
	RunLeaseID       pgtype.UUID `json:"run_lease_id"`
	WorkerInstanceID pgtype.UUID `json:"worker_instance_id"`
}

type GetRunRestorePayloadRow struct {
	RuntimeCheckpointID  pgtype.UUID        `json:"runtime_checkpoint_id"`
	Manifest             []byte             `json:"manifest"`
	RunWaitID            pgtype.UUID        `json:"run_wait_id"`
	RunWaitCorrelationID string             `json:"run_wait_correlation_id"`
	RunWaitKind          RunWaitKind        `json:"run_wait_kind"`
	RunWaitState         RunWaitState       `json:"run_wait_state"`
	StreamName           pgtype.Text        `json:"stream_name"`
	StreamRecordSequence pgtype.Int8        `json:"stream_record_sequence"`
	StreamRecordData     []byte             `json:"stream_record_data"`
	TokenState           NullTokenState     `json:"token_state"`
	TokenCompletionData  []byte             `json:"token_completion_data"`
	TimerFireAt          pgtype.Timestamptz `json:"timer_fire_at"`
}

func (q *Queries) GetRunRestorePayload(ctx context.Context, arg GetRunRestorePayloadParams) (GetRunRestorePayloadRow, error) {
	row := q.db.QueryRow(ctx, getRunRestorePayload,
		arg.OrgID,
		arg.RunID,
		arg.RunLeaseID,
		arg.WorkerInstanceID,
	)
	var i GetRunRestorePayloadRow
	err := row.Scan(
		&i.RuntimeCheckpointID,
		&i.Manifest,
		&i.RunWaitID,
		&i.RunWaitCorrelationID,
		&i.RunWaitKind,
		&i.RunWaitState,
		&i.StreamName,
		&i.StreamRecordSequence,
		&i.StreamRecordData,
		&i.TokenState,
		&i.TokenCompletionData,
		&i.TimerFireAt,
	)
	return i, err
}
