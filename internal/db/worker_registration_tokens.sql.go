// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: worker_registration_tokens.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const ensureDefaultWorkerRegistrationToken = `-- name: EnsureDefaultWorkerRegistrationToken :one
WITH default_group AS (
    SELECT worker_groups.id,
           worker_groups.project_id,
           worker_groups.environment_id
      FROM worker_groups
      JOIN projects ON projects.org_id = worker_groups.org_id
                   AND projects.id = worker_groups.project_id
                   AND projects.is_default
                   AND projects.archived_at IS NULL
      JOIN environments ON environments.org_id = worker_groups.org_id
                       AND environments.project_id = worker_groups.project_id
                       AND environments.id = worker_groups.environment_id
                       AND environments.is_default
                       AND environments.archived_at IS NULL
     WHERE worker_groups.org_id = $1
       AND worker_groups.slug = 'default'
       AND worker_groups.archived_at IS NULL
     LIMIT 1
),
upserted AS (
    INSERT INTO worker_registration_tokens (id, org_id, project_id, environment_id, worker_group_id, token_hash)
    SELECT
        $2,
        $1,
        default_group.project_id,
        default_group.environment_id,
        default_group.id,
        $3
      FROM default_group
    ON CONFLICT (token_hash) DO UPDATE
       SET org_id = excluded.org_id,
           project_id = excluded.project_id,
           environment_id = excluded.environment_id,
           worker_group_id = excluded.worker_group_id,
           revoked_at = NULL
    RETURNING id, org_id, project_id, environment_id, worker_group_id, token_hash, created_at, last_used_at, last_used_by_worker_host_id, revoked_at
),
revoked AS (
    UPDATE worker_registration_tokens
       SET revoked_at = now()
      FROM upserted
     WHERE worker_registration_tokens.org_id = upserted.org_id
       AND worker_registration_tokens.project_id = upserted.project_id
       AND worker_registration_tokens.environment_id = upserted.environment_id
       AND worker_registration_tokens.worker_group_id = upserted.worker_group_id
       AND worker_registration_tokens.token_hash <> upserted.token_hash
       AND worker_registration_tokens.revoked_at IS NULL
    RETURNING worker_registration_tokens.id
)
SELECT id, org_id, project_id, environment_id, worker_group_id, token_hash, created_at, last_used_at, last_used_by_worker_host_id, revoked_at FROM upserted
`

type EnsureDefaultWorkerRegistrationTokenParams struct {
	OrgID     pgtype.UUID `json:"org_id"`
	ID        pgtype.UUID `json:"id"`
	TokenHash []byte      `json:"token_hash"`
}

type EnsureDefaultWorkerRegistrationTokenRow struct {
	ID                     pgtype.UUID        `json:"id"`
	OrgID                  pgtype.UUID        `json:"org_id"`
	ProjectID              pgtype.UUID        `json:"project_id"`
	EnvironmentID          pgtype.UUID        `json:"environment_id"`
	WorkerGroupID          pgtype.UUID        `json:"worker_group_id"`
	TokenHash              []byte             `json:"token_hash"`
	CreatedAt              pgtype.Timestamptz `json:"created_at"`
	LastUsedAt             pgtype.Timestamptz `json:"last_used_at"`
	LastUsedByWorkerHostID pgtype.Text        `json:"last_used_by_worker_host_id"`
	RevokedAt              pgtype.Timestamptz `json:"revoked_at"`
}

func (q *Queries) EnsureDefaultWorkerRegistrationToken(ctx context.Context, arg EnsureDefaultWorkerRegistrationTokenParams) (EnsureDefaultWorkerRegistrationTokenRow, error) {
	row := q.db.QueryRow(ctx, ensureDefaultWorkerRegistrationToken, arg.OrgID, arg.ID, arg.TokenHash)
	var i EnsureDefaultWorkerRegistrationTokenRow
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.WorkerGroupID,
		&i.TokenHash,
		&i.CreatedAt,
		&i.LastUsedAt,
		&i.LastUsedByWorkerHostID,
		&i.RevokedAt,
	)
	return i, err
}
