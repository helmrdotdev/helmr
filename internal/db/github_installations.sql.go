// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: github_installations.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const deleteGitHubInstallation = `-- name: DeleteGitHubInstallation :one
UPDATE github_app_installations
   SET deleted_at = now(),
       suspended_at = NULL,
       updated_at = now()
 WHERE org_id = $1
   AND installation_id = $2
RETURNING id, org_id, installation_id, account_login, account_type, repository_selection, html_url, suspended_at, deleted_at, created_at, updated_at
`

type DeleteGitHubInstallationParams struct {
	OrgID          pgtype.UUID `json:"org_id"`
	InstallationID int64       `json:"installation_id"`
}

func (q *Queries) DeleteGitHubInstallation(ctx context.Context, arg DeleteGitHubInstallationParams) (GitHubAppInstallation, error) {
	row := q.db.QueryRow(ctx, deleteGitHubInstallation, arg.OrgID, arg.InstallationID)
	var i GitHubAppInstallation
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.InstallationID,
		&i.AccountLogin,
		&i.AccountType,
		&i.RepositorySelection,
		&i.HtmlUrl,
		&i.SuspendedAt,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteGitHubInstallationByInstallationID = `-- name: DeleteGitHubInstallationByInstallationID :many
UPDATE github_app_installations
   SET deleted_at = now(),
       suspended_at = NULL,
       updated_at = now()
 WHERE installation_id = $1
RETURNING id, org_id, installation_id, account_login, account_type, repository_selection, html_url, suspended_at, deleted_at, created_at, updated_at
`

func (q *Queries) DeleteGitHubInstallationByInstallationID(ctx context.Context, installationID int64) ([]GitHubAppInstallation, error) {
	rows, err := q.db.Query(ctx, deleteGitHubInstallationByInstallationID, installationID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GitHubAppInstallation
	for rows.Next() {
		var i GitHubAppInstallation
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.InstallationID,
			&i.AccountLogin,
			&i.AccountType,
			&i.RepositorySelection,
			&i.HtmlUrl,
			&i.SuspendedAt,
			&i.DeletedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getKnownGitHubInstallationByInstallationID = `-- name: GetKnownGitHubInstallationByInstallationID :one
SELECT id, org_id, installation_id, account_login, account_type, repository_selection, html_url, suspended_at, deleted_at, created_at, updated_at
  FROM github_app_installations
 WHERE installation_id = $1
   AND deleted_at IS NULL
 ORDER BY updated_at DESC
 LIMIT 1
`

func (q *Queries) GetKnownGitHubInstallationByInstallationID(ctx context.Context, installationID int64) (GitHubAppInstallation, error) {
	row := q.db.QueryRow(ctx, getKnownGitHubInstallationByInstallationID, installationID)
	var i GitHubAppInstallation
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.InstallationID,
		&i.AccountLogin,
		&i.AccountType,
		&i.RepositorySelection,
		&i.HtmlUrl,
		&i.SuspendedAt,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listGitHubInstallations = `-- name: ListGitHubInstallations :many
SELECT id, org_id, installation_id, account_login, account_type, repository_selection, html_url, suspended_at, deleted_at, created_at, updated_at
  FROM github_app_installations
 WHERE org_id = $1
   AND deleted_at IS NULL
 ORDER BY lower(account_login), updated_at DESC
`

func (q *Queries) ListGitHubInstallations(ctx context.Context, orgID pgtype.UUID) ([]GitHubAppInstallation, error) {
	rows, err := q.db.Query(ctx, listGitHubInstallations, orgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GitHubAppInstallation
	for rows.Next() {
		var i GitHubAppInstallation
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.InstallationID,
			&i.AccountLogin,
			&i.AccountType,
			&i.RepositorySelection,
			&i.HtmlUrl,
			&i.SuspendedAt,
			&i.DeletedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const suspendGitHubInstallation = `-- name: SuspendGitHubInstallation :one
UPDATE github_app_installations
   SET suspended_at = now(),
       updated_at = now()
 WHERE org_id = $1
   AND installation_id = $2
RETURNING id, org_id, installation_id, account_login, account_type, repository_selection, html_url, suspended_at, deleted_at, created_at, updated_at
`

type SuspendGitHubInstallationParams struct {
	OrgID          pgtype.UUID `json:"org_id"`
	InstallationID int64       `json:"installation_id"`
}

func (q *Queries) SuspendGitHubInstallation(ctx context.Context, arg SuspendGitHubInstallationParams) (GitHubAppInstallation, error) {
	row := q.db.QueryRow(ctx, suspendGitHubInstallation, arg.OrgID, arg.InstallationID)
	var i GitHubAppInstallation
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.InstallationID,
		&i.AccountLogin,
		&i.AccountType,
		&i.RepositorySelection,
		&i.HtmlUrl,
		&i.SuspendedAt,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const suspendGitHubInstallationByInstallationID = `-- name: SuspendGitHubInstallationByInstallationID :many
UPDATE github_app_installations
   SET suspended_at = now(),
       updated_at = now()
 WHERE installation_id = $1
   AND deleted_at IS NULL
RETURNING id, org_id, installation_id, account_login, account_type, repository_selection, html_url, suspended_at, deleted_at, created_at, updated_at
`

func (q *Queries) SuspendGitHubInstallationByInstallationID(ctx context.Context, installationID int64) ([]GitHubAppInstallation, error) {
	rows, err := q.db.Query(ctx, suspendGitHubInstallationByInstallationID, installationID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GitHubAppInstallation
	for rows.Next() {
		var i GitHubAppInstallation
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.InstallationID,
			&i.AccountLogin,
			&i.AccountType,
			&i.RepositorySelection,
			&i.HtmlUrl,
			&i.SuspendedAt,
			&i.DeletedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertGitHubInstallation = `-- name: UpsertGitHubInstallation :one
INSERT INTO github_app_installations (
    id,
    org_id,
    installation_id,
    account_login,
    account_type,
    repository_selection,
    html_url
) VALUES (
    $1, $2, $3, $4, $5, $6, $7
)
ON CONFLICT (org_id, installation_id) DO UPDATE
   SET account_login = EXCLUDED.account_login,
       account_type = EXCLUDED.account_type,
       repository_selection = EXCLUDED.repository_selection,
       html_url = EXCLUDED.html_url,
       suspended_at = NULL,
       deleted_at = NULL,
       updated_at = now()
RETURNING id, org_id, installation_id, account_login, account_type, repository_selection, html_url, suspended_at, deleted_at, created_at, updated_at
`

type UpsertGitHubInstallationParams struct {
	ID                  pgtype.UUID `json:"id"`
	OrgID               pgtype.UUID `json:"org_id"`
	InstallationID      int64       `json:"installation_id"`
	AccountLogin        string      `json:"account_login"`
	AccountType         string      `json:"account_type"`
	RepositorySelection pgtype.Text `json:"repository_selection"`
	HtmlUrl             pgtype.Text `json:"html_url"`
}

func (q *Queries) UpsertGitHubInstallation(ctx context.Context, arg UpsertGitHubInstallationParams) (GitHubAppInstallation, error) {
	row := q.db.QueryRow(ctx, upsertGitHubInstallation,
		arg.ID,
		arg.OrgID,
		arg.InstallationID,
		arg.AccountLogin,
		arg.AccountType,
		arg.RepositorySelection,
		arg.HtmlUrl,
	)
	var i GitHubAppInstallation
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.InstallationID,
		&i.AccountLogin,
		&i.AccountType,
		&i.RepositorySelection,
		&i.HtmlUrl,
		&i.SuspendedAt,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
