// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: worker_pool_registration_tokens.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const ensureDefaultWorkerPoolRegistrationToken = `-- name: EnsureDefaultWorkerPoolRegistrationToken :one
WITH default_pool AS (
    SELECT worker_pools.id,
           worker_pools.project_id,
           worker_pools.environment_id
      FROM worker_pools
      JOIN projects ON projects.org_id = worker_pools.org_id
                   AND projects.id = worker_pools.project_id
                   AND projects.is_default
                   AND projects.archived_at IS NULL
      JOIN environments ON environments.org_id = worker_pools.org_id
                       AND environments.project_id = worker_pools.project_id
                       AND environments.id = worker_pools.environment_id
                       AND environments.is_default
                       AND environments.archived_at IS NULL
     WHERE worker_pools.org_id = $1
       AND worker_pools.is_default
       AND worker_pools.archived_at IS NULL
     LIMIT 1
),
upserted AS (
    INSERT INTO worker_pool_registration_tokens (id, org_id, project_id, environment_id, worker_pool_id, token_hash)
    SELECT
        $2,
        $1,
        default_pool.project_id,
        default_pool.environment_id,
        default_pool.id,
        $3
      FROM default_pool
    ON CONFLICT (token_hash) DO UPDATE
       SET org_id = excluded.org_id,
           project_id = excluded.project_id,
           environment_id = excluded.environment_id,
           worker_pool_id = excluded.worker_pool_id,
           revoked_at = NULL
    RETURNING id, org_id, project_id, environment_id, worker_pool_id, token_hash, created_at, last_used_at, last_used_by_worker_id, revoked_at
),
revoked AS (
    UPDATE worker_pool_registration_tokens
       SET revoked_at = now()
      FROM upserted
     WHERE worker_pool_registration_tokens.org_id = upserted.org_id
       AND worker_pool_registration_tokens.project_id = upserted.project_id
       AND worker_pool_registration_tokens.environment_id = upserted.environment_id
       AND worker_pool_registration_tokens.worker_pool_id = upserted.worker_pool_id
       AND worker_pool_registration_tokens.token_hash <> upserted.token_hash
       AND worker_pool_registration_tokens.revoked_at IS NULL
    RETURNING worker_pool_registration_tokens.id
)
SELECT id, org_id, project_id, environment_id, worker_pool_id, token_hash, created_at, last_used_at, last_used_by_worker_id, revoked_at FROM upserted
`

type EnsureDefaultWorkerPoolRegistrationTokenParams struct {
	OrgID     pgtype.UUID `json:"org_id"`
	ID        pgtype.UUID `json:"id"`
	TokenHash []byte      `json:"token_hash"`
}

type EnsureDefaultWorkerPoolRegistrationTokenRow struct {
	ID                 pgtype.UUID        `json:"id"`
	OrgID              pgtype.UUID        `json:"org_id"`
	ProjectID          pgtype.UUID        `json:"project_id"`
	EnvironmentID      pgtype.UUID        `json:"environment_id"`
	WorkerPoolID       pgtype.UUID        `json:"worker_pool_id"`
	TokenHash          []byte             `json:"token_hash"`
	CreatedAt          pgtype.Timestamptz `json:"created_at"`
	LastUsedAt         pgtype.Timestamptz `json:"last_used_at"`
	LastUsedByWorkerID pgtype.Text        `json:"last_used_by_worker_id"`
	RevokedAt          pgtype.Timestamptz `json:"revoked_at"`
}

func (q *Queries) EnsureDefaultWorkerPoolRegistrationToken(ctx context.Context, arg EnsureDefaultWorkerPoolRegistrationTokenParams) (EnsureDefaultWorkerPoolRegistrationTokenRow, error) {
	row := q.db.QueryRow(ctx, ensureDefaultWorkerPoolRegistrationToken, arg.OrgID, arg.ID, arg.TokenHash)
	var i EnsureDefaultWorkerPoolRegistrationTokenRow
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.WorkerPoolID,
		&i.TokenHash,
		&i.CreatedAt,
		&i.LastUsedAt,
		&i.LastUsedByWorkerID,
		&i.RevokedAt,
	)
	return i, err
}
