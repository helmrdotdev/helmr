// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: stream_waits.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createStreamWait = `-- name: CreateStreamWait :one
INSERT INTO stream_waits (
    id,
    org_id,
    cell_id,
    project_id,
    environment_id,
    run_wait_id,
    stream_id,
    after_sequence,
    correlation_id
)
SELECT $1,
       run_waits.org_id,
       run_waits.cell_id,
       run_waits.project_id,
       run_waits.environment_id,
       run_waits.id,
       streams.id,
       $2::bigint,
       COALESCE($3::text, '')
  FROM run_waits
  JOIN streams ON streams.org_id = run_waits.org_id
              AND streams.project_id = run_waits.project_id
              AND streams.environment_id = run_waits.environment_id
              AND streams.id = $4
 WHERE run_waits.org_id = $5
   AND run_waits.project_id = $6
   AND run_waits.environment_id = $7
   AND run_waits.id = $8
   AND run_waits.kind = 'stream'
RETURNING id, org_id, cell_id, project_id, environment_id, run_wait_id, stream_id, after_sequence, correlation_id, matched_record_id, cursor_advanced_at, created_at
`

type CreateStreamWaitParams struct {
	ID            pgtype.UUID `json:"id"`
	AfterSequence int64       `json:"after_sequence"`
	CorrelationID string      `json:"correlation_id"`
	StreamID      pgtype.UUID `json:"stream_id"`
	OrgID         pgtype.UUID `json:"org_id"`
	ProjectID     pgtype.UUID `json:"project_id"`
	EnvironmentID pgtype.UUID `json:"environment_id"`
	RunWaitID     pgtype.UUID `json:"run_wait_id"`
}

func (q *Queries) CreateStreamWait(ctx context.Context, arg CreateStreamWaitParams) (StreamWait, error) {
	row := q.db.QueryRow(ctx, createStreamWait,
		arg.ID,
		arg.AfterSequence,
		arg.CorrelationID,
		arg.StreamID,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.RunWaitID,
	)
	var i StreamWait
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.CellID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.RunWaitID,
		&i.StreamID,
		&i.AfterSequence,
		&i.CorrelationID,
		&i.MatchedRecordID,
		&i.CursorAdvancedAt,
		&i.CreatedAt,
	)
	return i, err
}

const getStreamWaitForRunWait = `-- name: GetStreamWaitForRunWait :one
SELECT id, org_id, cell_id, project_id, environment_id, run_wait_id, stream_id, after_sequence, correlation_id, matched_record_id, cursor_advanced_at, created_at
  FROM stream_waits
 WHERE org_id = $1
   AND project_id = $2
   AND environment_id = $3
   AND run_wait_id = $4
`

type GetStreamWaitForRunWaitParams struct {
	OrgID         pgtype.UUID `json:"org_id"`
	ProjectID     pgtype.UUID `json:"project_id"`
	EnvironmentID pgtype.UUID `json:"environment_id"`
	RunWaitID     pgtype.UUID `json:"run_wait_id"`
}

func (q *Queries) GetStreamWaitForRunWait(ctx context.Context, arg GetStreamWaitForRunWaitParams) (StreamWait, error) {
	row := q.db.QueryRow(ctx, getStreamWaitForRunWait,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.RunWaitID,
	)
	var i StreamWait
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.CellID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.RunWaitID,
		&i.StreamID,
		&i.AfterSequence,
		&i.CorrelationID,
		&i.MatchedRecordID,
		&i.CursorAdvancedAt,
		&i.CreatedAt,
	)
	return i, err
}
