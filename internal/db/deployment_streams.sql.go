// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: deployment_streams.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getDeploymentStreamByName = `-- name: GetDeploymentStreamByName :one
SELECT id, org_id, project_id, environment_id, deployment_id, task_id, name, direction, schema_fingerprint, schema_json, metadata, created_at
  FROM deployment_streams
 WHERE org_id = $1
   AND project_id = $2
   AND environment_id = $3
   AND deployment_id = $4
   AND task_id = $5
   AND name = $6
   AND direction = $7::stream_direction
`

type GetDeploymentStreamByNameParams struct {
	OrgID         pgtype.UUID     `json:"org_id"`
	ProjectID     pgtype.UUID     `json:"project_id"`
	EnvironmentID pgtype.UUID     `json:"environment_id"`
	DeploymentID  pgtype.UUID     `json:"deployment_id"`
	TaskID        string          `json:"task_id"`
	Name          string          `json:"name"`
	Direction     StreamDirection `json:"direction"`
}

func (q *Queries) GetDeploymentStreamByName(ctx context.Context, arg GetDeploymentStreamByNameParams) (DeploymentStream, error) {
	row := q.db.QueryRow(ctx, getDeploymentStreamByName,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.DeploymentID,
		arg.TaskID,
		arg.Name,
		arg.Direction,
	)
	var i DeploymentStream
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.DeploymentID,
		&i.TaskID,
		&i.Name,
		&i.Direction,
		&i.SchemaFingerprint,
		&i.SchemaJson,
		&i.Metadata,
		&i.CreatedAt,
	)
	return i, err
}

const listDeploymentStreamsForDeployment = `-- name: ListDeploymentStreamsForDeployment :many
SELECT id, org_id, project_id, environment_id, deployment_id, task_id, name, direction, schema_fingerprint, schema_json, metadata, created_at
  FROM deployment_streams
 WHERE org_id = $1
   AND project_id = $2
   AND environment_id = $3
   AND deployment_id = $4
 ORDER BY task_id ASC, name ASC, direction ASC
`

type ListDeploymentStreamsForDeploymentParams struct {
	OrgID         pgtype.UUID `json:"org_id"`
	ProjectID     pgtype.UUID `json:"project_id"`
	EnvironmentID pgtype.UUID `json:"environment_id"`
	DeploymentID  pgtype.UUID `json:"deployment_id"`
}

func (q *Queries) ListDeploymentStreamsForDeployment(ctx context.Context, arg ListDeploymentStreamsForDeploymentParams) ([]DeploymentStream, error) {
	rows, err := q.db.Query(ctx, listDeploymentStreamsForDeployment,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.DeploymentID,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []DeploymentStream
	for rows.Next() {
		var i DeploymentStream
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.ProjectID,
			&i.EnvironmentID,
			&i.DeploymentID,
			&i.TaskID,
			&i.Name,
			&i.Direction,
			&i.SchemaFingerprint,
			&i.SchemaJson,
			&i.Metadata,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listDeploymentStreamsForTask = `-- name: ListDeploymentStreamsForTask :many
SELECT id, org_id, project_id, environment_id, deployment_id, task_id, name, direction, schema_fingerprint, schema_json, metadata, created_at
  FROM deployment_streams
 WHERE org_id = $1
   AND project_id = $2
   AND environment_id = $3
   AND deployment_id = $4
   AND task_id = $5
 ORDER BY name ASC, direction ASC
`

type ListDeploymentStreamsForTaskParams struct {
	OrgID         pgtype.UUID `json:"org_id"`
	ProjectID     pgtype.UUID `json:"project_id"`
	EnvironmentID pgtype.UUID `json:"environment_id"`
	DeploymentID  pgtype.UUID `json:"deployment_id"`
	TaskID        string      `json:"task_id"`
}

func (q *Queries) ListDeploymentStreamsForTask(ctx context.Context, arg ListDeploymentStreamsForTaskParams) ([]DeploymentStream, error) {
	rows, err := q.db.Query(ctx, listDeploymentStreamsForTask,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.DeploymentID,
		arg.TaskID,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []DeploymentStream
	for rows.Next() {
		var i DeploymentStream
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.ProjectID,
			&i.EnvironmentID,
			&i.DeploymentID,
			&i.TaskID,
			&i.Name,
			&i.Direction,
			&i.SchemaFingerprint,
			&i.SchemaJson,
			&i.Metadata,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertDeploymentStream = `-- name: UpsertDeploymentStream :one
INSERT INTO deployment_streams (
    id,
    org_id,
    project_id,
    environment_id,
    deployment_id,
    task_id,
    name,
    direction,
    schema_fingerprint,
    schema_json,
    metadata
)
VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6,
    $7,
    $8::stream_direction,
    COALESCE($9::text, ''),
    COALESCE($10::jsonb, 'null'::jsonb),
    COALESCE($11::jsonb, '{}'::jsonb)
)
ON CONFLICT (org_id, deployment_id, task_id, name, direction)
DO UPDATE SET
    schema_fingerprint = EXCLUDED.schema_fingerprint,
    schema_json = EXCLUDED.schema_json,
    metadata = EXCLUDED.metadata
RETURNING id, org_id, project_id, environment_id, deployment_id, task_id, name, direction, schema_fingerprint, schema_json, metadata, created_at
`

type UpsertDeploymentStreamParams struct {
	ID                pgtype.UUID     `json:"id"`
	OrgID             pgtype.UUID     `json:"org_id"`
	ProjectID         pgtype.UUID     `json:"project_id"`
	EnvironmentID     pgtype.UUID     `json:"environment_id"`
	DeploymentID      pgtype.UUID     `json:"deployment_id"`
	TaskID            string          `json:"task_id"`
	Name              string          `json:"name"`
	Direction         StreamDirection `json:"direction"`
	SchemaFingerprint string          `json:"schema_fingerprint"`
	SchemaJson        []byte          `json:"schema_json"`
	Metadata          []byte          `json:"metadata"`
}

func (q *Queries) UpsertDeploymentStream(ctx context.Context, arg UpsertDeploymentStreamParams) (DeploymentStream, error) {
	row := q.db.QueryRow(ctx, upsertDeploymentStream,
		arg.ID,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.DeploymentID,
		arg.TaskID,
		arg.Name,
		arg.Direction,
		arg.SchemaFingerprint,
		arg.SchemaJson,
		arg.Metadata,
	)
	var i DeploymentStream
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.DeploymentID,
		&i.TaskID,
		&i.Name,
		&i.Direction,
		&i.SchemaFingerprint,
		&i.SchemaJson,
		&i.Metadata,
		&i.CreatedAt,
	)
	return i, err
}
