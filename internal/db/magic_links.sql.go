// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: magic_links.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const consumeMagicLink = `-- name: ConsumeMagicLink :execrows
UPDATE magic_links
   SET consumed_at = now(),
       consumed_by_user_id = $1
 WHERE id = $2
   AND sent_at IS NOT NULL
   AND consumed_at IS NULL
   AND revoked_at IS NULL
   AND expires_at > now()
`

type ConsumeMagicLinkParams struct {
	ConsumedByUserID pgtype.UUID `json:"consumed_by_user_id"`
	ID               pgtype.UUID `json:"id"`
}

func (q *Queries) ConsumeMagicLink(ctx context.Context, arg ConsumeMagicLinkParams) (int64, error) {
	result, err := q.db.Exec(ctx, consumeMagicLink, arg.ConsumedByUserID, arg.ID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const countRecentMagicLinks = `-- name: CountRecentMagicLinks :one
SELECT count(*)
 FROM magic_links
 WHERE purpose = $1::magic_link_purpose
   AND email = $2
   AND delivery_failed_at IS NULL
   AND created_at >= $3
`

type CountRecentMagicLinksParams struct {
	Purpose MagicLinkPurpose   `json:"purpose"`
	Email   string             `json:"email"`
	Since   pgtype.Timestamptz `json:"since"`
}

func (q *Queries) CountRecentMagicLinks(ctx context.Context, arg CountRecentMagicLinksParams) (int64, error) {
	row := q.db.QueryRow(ctx, countRecentMagicLinks, arg.Purpose, arg.Email, arg.Since)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createMagicLink = `-- name: CreateMagicLink :one
INSERT INTO magic_links (
    id,
    purpose,
    token_hash,
    email,
    org_id,
    invitation_id,
    redirect_after,
    expires_at
)
VALUES (
    $1,
    $2::magic_link_purpose,
    $3,
    $4,
    $5,
    $6,
    $7,
    $8
)
RETURNING id, purpose, token_hash, email, org_id, invitation_id, redirect_after, created_at, sent_at, delivery_failed_at, expires_at, consumed_at, consumed_by_user_id, revoked_at
`

type CreateMagicLinkParams struct {
	ID            pgtype.UUID        `json:"id"`
	Purpose       MagicLinkPurpose   `json:"purpose"`
	TokenHash     []byte             `json:"token_hash"`
	Email         string             `json:"email"`
	OrgID         pgtype.UUID        `json:"org_id"`
	InvitationID  pgtype.UUID        `json:"invitation_id"`
	RedirectAfter pgtype.Text        `json:"redirect_after"`
	ExpiresAt     pgtype.Timestamptz `json:"expires_at"`
}

func (q *Queries) CreateMagicLink(ctx context.Context, arg CreateMagicLinkParams) (MagicLink, error) {
	row := q.db.QueryRow(ctx, createMagicLink,
		arg.ID,
		arg.Purpose,
		arg.TokenHash,
		arg.Email,
		arg.OrgID,
		arg.InvitationID,
		arg.RedirectAfter,
		arg.ExpiresAt,
	)
	var i MagicLink
	err := row.Scan(
		&i.ID,
		&i.Purpose,
		&i.TokenHash,
		&i.Email,
		&i.OrgID,
		&i.InvitationID,
		&i.RedirectAfter,
		&i.CreatedAt,
		&i.SentAt,
		&i.DeliveryFailedAt,
		&i.ExpiresAt,
		&i.ConsumedAt,
		&i.ConsumedByUserID,
		&i.RevokedAt,
	)
	return i, err
}

const getActiveMagicLinkByTokenHash = `-- name: GetActiveMagicLinkByTokenHash :one
SELECT id,
       purpose,
       email,
       org_id,
       invitation_id,
       redirect_after,
       expires_at
  FROM magic_links
 WHERE token_hash = $1
   AND sent_at IS NOT NULL
   AND consumed_at IS NULL
   AND revoked_at IS NULL
   AND expires_at > now()
 FOR UPDATE
`

type GetActiveMagicLinkByTokenHashRow struct {
	ID            pgtype.UUID        `json:"id"`
	Purpose       MagicLinkPurpose   `json:"purpose"`
	Email         string             `json:"email"`
	OrgID         pgtype.UUID        `json:"org_id"`
	InvitationID  pgtype.UUID        `json:"invitation_id"`
	RedirectAfter pgtype.Text        `json:"redirect_after"`
	ExpiresAt     pgtype.Timestamptz `json:"expires_at"`
}

func (q *Queries) GetActiveMagicLinkByTokenHash(ctx context.Context, tokenHash []byte) (GetActiveMagicLinkByTokenHashRow, error) {
	row := q.db.QueryRow(ctx, getActiveMagicLinkByTokenHash, tokenHash)
	var i GetActiveMagicLinkByTokenHashRow
	err := row.Scan(
		&i.ID,
		&i.Purpose,
		&i.Email,
		&i.OrgID,
		&i.InvitationID,
		&i.RedirectAfter,
		&i.ExpiresAt,
	)
	return i, err
}

const getMagicLinkLoginUser = `-- name: GetMagicLinkLoginUser :one
SELECT users.id, users.display_name, users.profile_image_url, users.primary_email, users.disabled_at, users.created_at, users.updated_at
  FROM users
 WHERE lower(users.primary_email) = $1
   AND users.disabled_at IS NULL
 LIMIT 1
`

func (q *Queries) GetMagicLinkLoginUser(ctx context.Context, email pgtype.Text) (User, error) {
	row := q.db.QueryRow(ctx, getMagicLinkLoginUser, email)
	var i User
	err := row.Scan(
		&i.ID,
		&i.DisplayName,
		&i.ProfileImageUrl,
		&i.PrimaryEmail,
		&i.DisabledAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const markMagicLinkDeliveryFailed = `-- name: MarkMagicLinkDeliveryFailed :execrows
UPDATE magic_links
   SET delivery_failed_at = now(),
       revoked_at = now()
 WHERE id = $1
   AND sent_at IS NULL
   AND delivery_failed_at IS NULL
   AND consumed_at IS NULL
   AND revoked_at IS NULL
`

func (q *Queries) MarkMagicLinkDeliveryFailed(ctx context.Context, id pgtype.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, markMagicLinkDeliveryFailed, id)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const markMagicLinkSent = `-- name: MarkMagicLinkSent :execrows
UPDATE magic_links AS current_link
   SET sent_at = now(),
       revoked_at = CASE
           WHEN EXISTS (
               SELECT 1
                 FROM magic_links AS newer_link
                WHERE newer_link.purpose = current_link.purpose
                  AND newer_link.email = current_link.email
                  AND newer_link.org_id IS NOT DISTINCT FROM current_link.org_id
                  AND newer_link.invitation_id IS NOT DISTINCT FROM current_link.invitation_id
                  AND newer_link.created_at > current_link.created_at
                  AND newer_link.sent_at IS NOT NULL
           )
           THEN now()
           ELSE current_link.revoked_at
       END
 WHERE current_link.id = $1
   AND current_link.sent_at IS NULL
   AND current_link.delivery_failed_at IS NULL
   AND current_link.consumed_at IS NULL
   AND current_link.revoked_at IS NULL
`

func (q *Queries) MarkMagicLinkSent(ctx context.Context, id pgtype.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, markMagicLinkSent, id)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const revokeOpenMagicLinksForRecipient = `-- name: RevokeOpenMagicLinksForRecipient :execrows
UPDATE magic_links
   SET revoked_at = now()
 WHERE magic_links.purpose = $1::magic_link_purpose
   AND magic_links.email = $2
   AND magic_links.org_id IS NOT DISTINCT FROM $3
   AND magic_links.invitation_id IS NOT DISTINCT FROM $4
   AND magic_links.id <> $5
   AND magic_links.created_at < (SELECT created_at FROM magic_links AS current_link WHERE current_link.id = $5)
   AND magic_links.sent_at IS NOT NULL
   AND magic_links.consumed_at IS NULL
   AND magic_links.revoked_at IS NULL
`

type RevokeOpenMagicLinksForRecipientParams struct {
	Purpose      MagicLinkPurpose `json:"purpose"`
	Email        string           `json:"email"`
	OrgID        pgtype.UUID      `json:"org_id"`
	InvitationID pgtype.UUID      `json:"invitation_id"`
	ExceptID     pgtype.UUID      `json:"except_id"`
}

func (q *Queries) RevokeOpenMagicLinksForRecipient(ctx context.Context, arg RevokeOpenMagicLinksForRecipientParams) (int64, error) {
	result, err := q.db.Exec(ctx, revokeOpenMagicLinksForRecipient,
		arg.Purpose,
		arg.Email,
		arg.OrgID,
		arg.InvitationID,
		arg.ExceptID,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const upsertMagicLinkAuthIdentity = `-- name: UpsertMagicLinkAuthIdentity :one
WITH existing_identity AS (
    UPDATE auth_identities AS identity
       SET email = $1,
           claims = $2,
           updated_at = now(),
           last_login_at = now()
     WHERE identity.provider = $3
       AND identity.subject = $4
     RETURNING user_id
),
upserted_user AS (
    INSERT INTO users (id, display_name, profile_image_url, primary_email)
    SELECT $5, $6, $7, $1
     WHERE NOT EXISTS (SELECT 1 FROM existing_identity)
    ON CONFLICT (lower(primary_email)) WHERE primary_email IS NOT NULL AND disabled_at IS NULL DO UPDATE
       SET primary_email = users.primary_email
     WHERE users.disabled_at IS NULL
    RETURNING id
),
target_user AS (
    SELECT user_id AS id FROM existing_identity
    UNION ALL
    SELECT id FROM upserted_user
),
inserted_identity AS (
    INSERT INTO auth_identities (
        id,
        user_id,
        provider,
        subject,
        email,
        claims,
        last_login_at
    )
    SELECT
        $8,
        target_user.id,
        $3,
        $4,
        $1,
        $2,
        now()
      FROM target_user
    ON CONFLICT (provider, subject) DO UPDATE
       SET email = EXCLUDED.email,
           claims = EXCLUDED.claims,
           updated_at = now(),
           last_login_at = now()
    RETURNING user_id
),
updated_user AS (
    UPDATE users
       SET display_name = $6,
           profile_image_url = COALESCE($7, users.profile_image_url),
           primary_email = $1,
           updated_at = now()
     WHERE id IN (SELECT user_id FROM inserted_identity)
    RETURNING id, display_name, profile_image_url, primary_email, disabled_at, created_at, updated_at
)
SELECT id, display_name, profile_image_url, primary_email, disabled_at, created_at, updated_at FROM updated_user
`

type UpsertMagicLinkAuthIdentityParams struct {
	Email            pgtype.Text `json:"email"`
	Claims           []byte      `json:"claims"`
	IdentityProvider string      `json:"identity_provider"`
	IdentitySubject  string      `json:"identity_subject"`
	UserID           pgtype.UUID `json:"user_id"`
	DisplayName      string      `json:"display_name"`
	ProfileImageUrl  pgtype.Text `json:"profile_image_url"`
	IdentityID       pgtype.UUID `json:"identity_id"`
}

type UpsertMagicLinkAuthIdentityRow struct {
	ID              pgtype.UUID        `json:"id"`
	DisplayName     string             `json:"display_name"`
	ProfileImageUrl pgtype.Text        `json:"profile_image_url"`
	PrimaryEmail    pgtype.Text        `json:"primary_email"`
	DisabledAt      pgtype.Timestamptz `json:"disabled_at"`
	CreatedAt       pgtype.Timestamptz `json:"created_at"`
	UpdatedAt       pgtype.Timestamptz `json:"updated_at"`
}

func (q *Queries) UpsertMagicLinkAuthIdentity(ctx context.Context, arg UpsertMagicLinkAuthIdentityParams) (UpsertMagicLinkAuthIdentityRow, error) {
	row := q.db.QueryRow(ctx, upsertMagicLinkAuthIdentity,
		arg.Email,
		arg.Claims,
		arg.IdentityProvider,
		arg.IdentitySubject,
		arg.UserID,
		arg.DisplayName,
		arg.ProfileImageUrl,
		arg.IdentityID,
	)
	var i UpsertMagicLinkAuthIdentityRow
	err := row.Scan(
		&i.ID,
		&i.DisplayName,
		&i.ProfileImageUrl,
		&i.PrimaryEmail,
		&i.DisabledAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
