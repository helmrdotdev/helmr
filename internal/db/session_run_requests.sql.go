// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: session_run_requests.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const claimDueSessionRunRequests = `-- name: ClaimDueSessionRunRequests :many
WITH eligible AS (
    SELECT id
      FROM session_run_requests
     WHERE status IN ('accepted', 'claimed')
       AND (
           status = 'accepted'
           OR claim_expires_at IS NULL
           OR claim_expires_at <= now()
       )
       AND next_attempt_at <= now()
       AND (
           $3::uuid IS NULL
           OR org_id = $3
       )
       AND (
           $4::uuid IS NULL
           OR project_id = $4
       )
       AND (
           $5::uuid IS NULL
           OR environment_id = $5
       )
       AND (
           $6::uuid IS NULL
           OR session_id = $6
       )
     ORDER BY next_attempt_at ASC, created_at ASC, id ASC
     LIMIT $7
     FOR UPDATE SKIP LOCKED
)
UPDATE session_run_requests
   SET status = 'claimed',
       attempts = attempts + 1,
       claimed_at = now(),
       claim_expires_at = now() + $1::interval,
       claim_owner = $2,
       updated_at = now()
  FROM eligible
 WHERE session_run_requests.id = eligible.id
RETURNING session_run_requests.id, session_run_requests.org_id, session_run_requests.project_id, session_run_requests.environment_id, session_run_requests.session_id, session_run_requests.stream_record_id, session_run_requests.stream_id, session_run_requests.cause_kind, session_run_requests.status, session_run_requests.attempts, session_run_requests.next_attempt_at, session_run_requests.last_error, session_run_requests.claimed_at, session_run_requests.claim_expires_at, session_run_requests.claim_owner, session_run_requests.run_id, session_run_requests.error_message, session_run_requests.created_at, session_run_requests.updated_at
`

type ClaimDueSessionRunRequestsParams struct {
	ClaimTtl      pgtype.Interval `json:"claim_ttl"`
	ClaimOwner    string          `json:"claim_owner"`
	OrgID         pgtype.UUID     `json:"org_id"`
	ProjectID     pgtype.UUID     `json:"project_id"`
	EnvironmentID pgtype.UUID     `json:"environment_id"`
	SessionID     pgtype.UUID     `json:"session_id"`
	LimitCount    int32           `json:"limit_count"`
}

func (q *Queries) ClaimDueSessionRunRequests(ctx context.Context, arg ClaimDueSessionRunRequestsParams) ([]SessionRunRequest, error) {
	rows, err := q.db.Query(ctx, claimDueSessionRunRequests,
		arg.ClaimTtl,
		arg.ClaimOwner,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.SessionID,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SessionRunRequest
	for rows.Next() {
		var i SessionRunRequest
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.ProjectID,
			&i.EnvironmentID,
			&i.SessionID,
			&i.StreamRecordID,
			&i.StreamID,
			&i.CauseKind,
			&i.Status,
			&i.Attempts,
			&i.NextAttemptAt,
			&i.LastError,
			&i.ClaimedAt,
			&i.ClaimExpiresAt,
			&i.ClaimOwner,
			&i.RunID,
			&i.ErrorMessage,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const ensureSessionRunRequestForStreamRecord = `-- name: EnsureSessionRunRequestForStreamRecord :one
INSERT INTO session_run_requests (
    id,
    org_id,
    project_id,
    environment_id,
    session_id,
    stream_record_id,
    stream_id,
    cause_kind
) VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6,
    $7,
    'stream_record'
)
ON CONFLICT (org_id, project_id, environment_id, stream_record_id)
DO UPDATE SET updated_at = session_run_requests.updated_at
RETURNING id, org_id, project_id, environment_id, session_id, stream_record_id, stream_id, cause_kind, status, attempts, next_attempt_at, last_error, claimed_at, claim_expires_at, claim_owner, run_id, error_message, created_at, updated_at
`

type EnsureSessionRunRequestForStreamRecordParams struct {
	ID             pgtype.UUID `json:"id"`
	OrgID          pgtype.UUID `json:"org_id"`
	ProjectID      pgtype.UUID `json:"project_id"`
	EnvironmentID  pgtype.UUID `json:"environment_id"`
	SessionID      pgtype.UUID `json:"session_id"`
	StreamRecordID pgtype.UUID `json:"stream_record_id"`
	StreamID       pgtype.UUID `json:"stream_id"`
}

func (q *Queries) EnsureSessionRunRequestForStreamRecord(ctx context.Context, arg EnsureSessionRunRequestForStreamRecordParams) (SessionRunRequest, error) {
	row := q.db.QueryRow(ctx, ensureSessionRunRequestForStreamRecord,
		arg.ID,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.SessionID,
		arg.StreamRecordID,
		arg.StreamID,
	)
	var i SessionRunRequest
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.SessionID,
		&i.StreamRecordID,
		&i.StreamID,
		&i.CauseKind,
		&i.Status,
		&i.Attempts,
		&i.NextAttemptAt,
		&i.LastError,
		&i.ClaimedAt,
		&i.ClaimExpiresAt,
		&i.ClaimOwner,
		&i.RunID,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const markSessionRunRequestConsumedByActiveRun = `-- name: MarkSessionRunRequestConsumedByActiveRun :one
UPDATE session_run_requests
   SET status = 'skipped',
       last_error = 'consumed_by_active_run',
       error_message = '',
       claimed_at = NULL,
       claim_expires_at = NULL,
       claim_owner = '',
       updated_at = now()
 WHERE org_id = $1
   AND project_id = $2
   AND environment_id = $3
   AND stream_record_id = $4
   AND status IN ('accepted', 'claimed')
RETURNING id, org_id, project_id, environment_id, session_id, stream_record_id, stream_id, cause_kind, status, attempts, next_attempt_at, last_error, claimed_at, claim_expires_at, claim_owner, run_id, error_message, created_at, updated_at
`

type MarkSessionRunRequestConsumedByActiveRunParams struct {
	OrgID          pgtype.UUID `json:"org_id"`
	ProjectID      pgtype.UUID `json:"project_id"`
	EnvironmentID  pgtype.UUID `json:"environment_id"`
	StreamRecordID pgtype.UUID `json:"stream_record_id"`
}

func (q *Queries) MarkSessionRunRequestConsumedByActiveRun(ctx context.Context, arg MarkSessionRunRequestConsumedByActiveRunParams) (SessionRunRequest, error) {
	row := q.db.QueryRow(ctx, markSessionRunRequestConsumedByActiveRun,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.StreamRecordID,
	)
	var i SessionRunRequest
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.SessionID,
		&i.StreamRecordID,
		&i.StreamID,
		&i.CauseKind,
		&i.Status,
		&i.Attempts,
		&i.NextAttemptAt,
		&i.LastError,
		&i.ClaimedAt,
		&i.ClaimExpiresAt,
		&i.ClaimOwner,
		&i.RunID,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const markSessionRunRequestCreated = `-- name: MarkSessionRunRequestCreated :one
UPDATE session_run_requests
   SET status = 'created',
       run_id = $1,
       last_error = '',
       error_message = '',
       claimed_at = NULL,
       claim_expires_at = NULL,
       claim_owner = '',
       updated_at = now()
 WHERE org_id = $2
	   AND project_id = $3
	   AND environment_id = $4
	   AND id = $5
	   AND status = 'claimed'
	   AND claim_owner = $6
	RETURNING id, org_id, project_id, environment_id, session_id, stream_record_id, stream_id, cause_kind, status, attempts, next_attempt_at, last_error, claimed_at, claim_expires_at, claim_owner, run_id, error_message, created_at, updated_at
`

type MarkSessionRunRequestCreatedParams struct {
	RunID         pgtype.UUID `json:"run_id"`
	OrgID         pgtype.UUID `json:"org_id"`
	ProjectID     pgtype.UUID `json:"project_id"`
	EnvironmentID pgtype.UUID `json:"environment_id"`
	ID            pgtype.UUID `json:"id"`
	ClaimOwner    string      `json:"claim_owner"`
}

func (q *Queries) MarkSessionRunRequestCreated(ctx context.Context, arg MarkSessionRunRequestCreatedParams) (SessionRunRequest, error) {
	row := q.db.QueryRow(ctx, markSessionRunRequestCreated,
		arg.RunID,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.ID,
		arg.ClaimOwner,
	)
	var i SessionRunRequest
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.SessionID,
		&i.StreamRecordID,
		&i.StreamID,
		&i.CauseKind,
		&i.Status,
		&i.Attempts,
		&i.NextAttemptAt,
		&i.LastError,
		&i.ClaimedAt,
		&i.ClaimExpiresAt,
		&i.ClaimOwner,
		&i.RunID,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const markSessionRunRequestFailed = `-- name: MarkSessionRunRequestFailed :one
UPDATE session_run_requests
   SET status = 'failed',
       last_error = $1,
       error_message = $1,
       claimed_at = NULL,
       claim_expires_at = NULL,
       claim_owner = '',
       updated_at = now()
 WHERE org_id = $2
	   AND project_id = $3
	   AND environment_id = $4
	   AND id = $5
	   AND status = 'claimed'
	   AND claim_owner = $6
	RETURNING id, org_id, project_id, environment_id, session_id, stream_record_id, stream_id, cause_kind, status, attempts, next_attempt_at, last_error, claimed_at, claim_expires_at, claim_owner, run_id, error_message, created_at, updated_at
`

type MarkSessionRunRequestFailedParams struct {
	Reason        string      `json:"reason"`
	OrgID         pgtype.UUID `json:"org_id"`
	ProjectID     pgtype.UUID `json:"project_id"`
	EnvironmentID pgtype.UUID `json:"environment_id"`
	ID            pgtype.UUID `json:"id"`
	ClaimOwner    string      `json:"claim_owner"`
}

func (q *Queries) MarkSessionRunRequestFailed(ctx context.Context, arg MarkSessionRunRequestFailedParams) (SessionRunRequest, error) {
	row := q.db.QueryRow(ctx, markSessionRunRequestFailed,
		arg.Reason,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.ID,
		arg.ClaimOwner,
	)
	var i SessionRunRequest
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.SessionID,
		&i.StreamRecordID,
		&i.StreamID,
		&i.CauseKind,
		&i.Status,
		&i.Attempts,
		&i.NextAttemptAt,
		&i.LastError,
		&i.ClaimedAt,
		&i.ClaimExpiresAt,
		&i.ClaimOwner,
		&i.RunID,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const markSessionRunRequestSkipped = `-- name: MarkSessionRunRequestSkipped :one
UPDATE session_run_requests
   SET status = 'skipped',
       last_error = $1,
       error_message = $1,
       claimed_at = NULL,
       claim_expires_at = NULL,
       claim_owner = '',
       updated_at = now()
 WHERE org_id = $2
	   AND project_id = $3
	   AND environment_id = $4
	   AND id = $5
	   AND status = 'claimed'
	   AND claim_owner = $6
	RETURNING id, org_id, project_id, environment_id, session_id, stream_record_id, stream_id, cause_kind, status, attempts, next_attempt_at, last_error, claimed_at, claim_expires_at, claim_owner, run_id, error_message, created_at, updated_at
`

type MarkSessionRunRequestSkippedParams struct {
	Reason        string      `json:"reason"`
	OrgID         pgtype.UUID `json:"org_id"`
	ProjectID     pgtype.UUID `json:"project_id"`
	EnvironmentID pgtype.UUID `json:"environment_id"`
	ID            pgtype.UUID `json:"id"`
	ClaimOwner    string      `json:"claim_owner"`
}

func (q *Queries) MarkSessionRunRequestSkipped(ctx context.Context, arg MarkSessionRunRequestSkippedParams) (SessionRunRequest, error) {
	row := q.db.QueryRow(ctx, markSessionRunRequestSkipped,
		arg.Reason,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.ID,
		arg.ClaimOwner,
	)
	var i SessionRunRequest
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.SessionID,
		&i.StreamRecordID,
		&i.StreamID,
		&i.CauseKind,
		&i.Status,
		&i.Attempts,
		&i.NextAttemptAt,
		&i.LastError,
		&i.ClaimedAt,
		&i.ClaimExpiresAt,
		&i.ClaimOwner,
		&i.RunID,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const releaseSessionRunRequestForRetry = `-- name: ReleaseSessionRunRequestForRetry :one
UPDATE session_run_requests
   SET status = 'accepted',
       next_attempt_at = now() + $1::interval,
       last_error = $2,
       error_message = $2,
       claimed_at = NULL,
       claim_expires_at = NULL,
       claim_owner = '',
       updated_at = now()
 WHERE org_id = $3
	   AND project_id = $4
	   AND environment_id = $5
	   AND id = $6
	   AND status = 'claimed'
	   AND claim_owner = $7
	RETURNING id, org_id, project_id, environment_id, session_id, stream_record_id, stream_id, cause_kind, status, attempts, next_attempt_at, last_error, claimed_at, claim_expires_at, claim_owner, run_id, error_message, created_at, updated_at
`

type ReleaseSessionRunRequestForRetryParams struct {
	RetryAfter    pgtype.Interval `json:"retry_after"`
	LastError     string          `json:"last_error"`
	OrgID         pgtype.UUID     `json:"org_id"`
	ProjectID     pgtype.UUID     `json:"project_id"`
	EnvironmentID pgtype.UUID     `json:"environment_id"`
	ID            pgtype.UUID     `json:"id"`
	ClaimOwner    string          `json:"claim_owner"`
}

func (q *Queries) ReleaseSessionRunRequestForRetry(ctx context.Context, arg ReleaseSessionRunRequestForRetryParams) (SessionRunRequest, error) {
	row := q.db.QueryRow(ctx, releaseSessionRunRequestForRetry,
		arg.RetryAfter,
		arg.LastError,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.ID,
		arg.ClaimOwner,
	)
	var i SessionRunRequest
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.SessionID,
		&i.StreamRecordID,
		&i.StreamID,
		&i.CauseKind,
		&i.Status,
		&i.Attempts,
		&i.NextAttemptAt,
		&i.LastError,
		&i.ClaimedAt,
		&i.ClaimExpiresAt,
		&i.ClaimOwner,
		&i.RunID,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
